# Generated by Django 5.0.6 on 2026-06-20 18:23
#
# Session 1174 PR-1: Add conversation_id to AgentExecution as the gating
# schema change for the agent-follow-up wake design
# (docs/handoffs/SESSION_1174_PRIMING_AGENT_FOLLOWUP.md).
#
# This migration intentionally contains ONLY the AddField. makemigrations
# initially swept in unrelated AlterField ops from other models (existing
# model-vs-migration drift in help_text and db_index). Those are out of
# scope for PR-1 and have been removed by hand to keep the change
# surgical and the rollback clean.

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0356_session_1171_backfill_spider_expires"),
    ]

    operations = [
        migrations.AddField(
            model_name="agentexecution",
            name="conversation_id",
            field=models.CharField(
                blank=True, db_index=True, max_length=64, null=True,
            ),
        ),
    ]
