---
title: "Engineering Playbook v0.11.0 Amendment Ratification Record (2026-07-28)"
status: active
authority: ratification-record
session_added: 3033
ratification_date: 2026-07-28
ratifier: chris
ratifier_verdict: "ship it"
routing: rigby-pa-chat joint SIGN (T1 tool-grounded verification with zoom-out ask per PLAYBOOK-6.10.7; all 5 dimensions AGREE with one `same_pr_mitigatable` refinement applied same-envelope) + Chris D-verdict via terminal ("ship it") after Phase 5 plain-English framing per PLAYBOOK-7.7.3
amendment_scope: playbook-minor-v0.11.0
amendment_class: MINOR (per PLAYBOOK-10.5.1 — 1 addition, 0 modifications, 0 removals)
parent_version: v0.10.0
parent_version_git_tag: playbook-v0.10.0
parent_version_commit_sha: PLACEHOLDER_FILLED_AT_MERGE
parent_version_ratification: RATIFICATION_2026-07-26_PLAYBOOK_V0_10_0
proposed_version: v0.11.0
proposed_git_tag: playbook-v0.11.0
predecessor_candidacy: three-cycle in-wild corpus surfaced across S3029 (Fold C `1st trigger` — DISCOVERY, tasks_ops 4th silent site caught) + S3030 (Fold C `2nd cycle, no discovery` — clean sweep confirmed drift class closed at source) + S3031 (Fold C `3rd cycle, no discovery` — clean sweep confirmed no ungated broadcast callers + no downstream `updated_at`-dependent consumers). S3031 close explicitly flagged "arguably ready to codify with the 'clean-sweep-confirms-closure' framing." S3033 open recommended Fold C 3rd-cycle codification (~30 min) as Lean 2; Chris ratified Lean 2 at session open.
head_at_amendment_draft: 9b74cc9059278c3f291499d107b622d484be0632
head_at_ratification: 180bd4e6567b
close_pr: 3755
cascade_pr: PLACEHOLDER_FILLED_AT_CASCADE
cascade_pr_merge_sha: PLACEHOLDER_FILLED_AT_CASCADE
sign_sessions:
  - S3033 T1 — Rigby joint SIGN dispatch with tool-grounded verify directives across 6 dimensions (D1 rule-text clarity/normativity; D2 evidence sufficiency; D3 scope containment; D4 EXTENDS discipline; D5 insertion + version-bump correctness; plus zoom-out pushback per PLAYBOOK-6.10.7). Rigby ran 6 `repo_tool` operations grounding the attestation: (TR-1) `search Fold C` in SESSION_3029 handoff — confirmed 1st-trigger + tasks_ops 4th-site discovery language; (TR-2) `search Fold C` in SESSION_3030 — confirmed 2nd-cycle no-discovery + "pattern useful even when it finds nothing" framing; (TR-3) `search Fold C` in SESSION_3031 — confirmed 3rd-cycle no-discovery + "ready-to-codify pressure elevated" language; (TR-4) `read_file docs/ENGINEERING_PLAYBOOK.md lines 1018-1078` — confirmed §7.7 body + rules 7.7.1-7.7.4 + clean insertion slot; (TR-5) `search PLAYBOOK-6.10.7` — confirmed parent-contract framing for EXTENDS relationship; (TR-6) `read_file RATIFICATION_2026-07-22_PLAYBOOK_V0_9_0.md` — confirmed single-rule envelope shape template. Anti-rubber-stamp gate PASS per PLAYBOOK-7.7.2 (tool_runs non-empty + line-cited).
  - S3033 T1 verdict — All 5 dimensions AGREE. Zoom-out fold: `same_pr_mitigatable` — (i)-(iv) enumeration risks ritualization if "shape being closed" isn't well-defined; "adjacent classes" boundary subjective. Mitigations applied same-envelope at §2.1: (a) Phase 2 plan MUST name shape signature (exact query predicate/ORM filter/function name/decorator/field invariant); (b) adjacent classes bounded to *same table/model OR same workflow stage OR same consumer boundary* — reviewer states axis inline; (c) enumeration prefixed "at minimum" to explicitly permit additional dimensions.
  - S3033 D-verdict — Chris ratified via Phase 5 plain-English routing per PLAYBOOK-7.7.3 ("do we lose anything?" NO + "is it more work later?" YES if skipped + ≤1 decision). Verdict: "ship it". Post-D-verdict ledger persistence: fold row appended to `logs/zoom_out_classifications.jsonl` via `record_zoom_out_concern` per PLAYBOOK-6.10.8 (post-D-verdict backfill per graceful-degradation clause — see §6 constitutional-debt note below).
rules_added:
  - PLAYBOOK-7.7.5
rules_modified: []
rules_removed: []
rule_count_before: 211
rule_count_after: 212
chapter_activation: "Chapter 7 §7.7 extension — 1 new [GR] rule under existing §7.7 authored-scope (§7.7 first authored at v0.10.0 with 4 rules 7.7.1-7.7.4). §7.7 grows from 4 rules to 5 rules; §7.7.5 EXTENDS §7.7.2 (SIGN evidence discipline) + §6.10.7 (joint SIGN zoom-out ask). Chapter 7 remains partially activated per stub-chapter frontmatter."
supersedes: none
superseded_by: (open; not expected — ratification records are frozen historical envelopes)
frozen: true
workspace_id: a9a16593-e0a4-44dc-8256-efc65d524b3c
workspace_name: "Architecture & Research"
workspace_ratification_deliverable_id: PLACEHOLDER_FILLED_POST_MERGE
d_verdicts:
  - D1 Rule-text clarity + normativity — AGREE (Rigby T1). Trigger condition tied to Phase-2 plan attribute; RFC-2119 consistent; clean-sweep clause explicit. Same_pr_mitigatable wording refinement applied at §2.1 — RATIFIED 2026-07-28 S3033
  - D2 Evidence sufficiency — AGREE (Rigby T1). 3-cycle corpus (1 DISCOVERY + 2 CLEAN) satisfies codification bar; requiring 2nd discovery would bias toward "gotchas that keep biting" and miss the prevent-misses value of clean-sweep-as-evidence — RATIFIED 2026-07-28 S3033
  - D3 Scope containment — AGREE (Rigby T1). §7.7.5 stays inside §7.7 spec→ship contract scope; tied to PLAYBOOK-7.7.1 Phase 2 Substantive Intent declaration + PLAYBOOK-7.7.1 Phase 7 A2 SIGN routing; no attempt to legislate general PR behavior — RATIFIED 2026-07-28 S3033
  - D4 EXTENDS discipline — AGREE (Rigby T1). EXTENDS 7.7.2 (adds required content element under specific trigger, doesn't replace general evidence discipline) + EXTENDS 6.10.7 (operationalizes a specific mandatory zoom-out for class trigger, doesn't replace general zoom-out contract). No SUPERSEDE risk — RATIFIED 2026-07-28 S3033
  - D5 Insertion + version-bump — AGREE (Rigby T1). Clean insertion after 7.7.4 (no ID collision). MINOR v0.10.0 → v0.11.0 for 1 new [GR] rule consistent with v0.6.0/v0.7.0/v0.8.0 single-rule cadence — RATIFIED 2026-07-28 S3033
  - D6 Zoom-out fold — `same_pr_mitigatable`. Mitigations applied same-envelope: shape-signature naming (Phase 2 plan MUST name), adjacent-class boundary (same table/model OR workflow stage OR consumer boundary), enumeration prefix ("at minimum"). Persisted to `logs/zoom_out_classifications.jsonl` under arc `playbook_v0_11_0_a2_sweep_drift_hardening` — RATIFIED 2026-07-28 S3033
prior_ratifications:
  - v0.10.0 (RATIFICATION_2026-07-26_PLAYBOOK_V0_10_0, S2981) — direct parent
  - v0.9.0 (RATIFICATION_2026-07-22_PLAYBOOK_V0_9_0, S2889) — envelope shape template
  - v0.8.0 (RATIFICATION_2026-07-14_PLAYBOOK_V0_8_0, S2786)
  - v0.7.0 (RATIFICATION_2026-07-13_PLAYBOOK_V0_7_0, S2778)
  - v0.6.0 (RATIFICATION_2026-07-11_PLAYBOOK_V0_6_0, S2766) — precedent for 1-rule MINOR under Chapter 7
constitutional_debt: []
zoom_out_ledger_rows_persisted:
  - {row: latest, session: 3033, arc: playbook_v0_11_0_a2_sweep_drift_hardening, classification: same_pr_mitigatable}
---

# Engineering Playbook v0.11.0 Amendment Ratification Record

**Amendment session:** S3033 · **Ratification date:** 2026-07-28 · **Ratifier:** Chris D. Levasseur · **Verdict:** "ship it"

## §1. Context

The S3026 → S3032 canonical-promotion arc closed at S3032 (PR #3753, commit `4e8a6a353`) across 5 axes: broadcast side (S3026-S3028), mutation side (S3029), historical drift (S3030), race elimination (S3031), and Rigby tool-surface coverage (S3032). Six PRs shipped in the S3030 + S3031 + S3032 terminal session.

Across the arc a distinct meta-pattern accreted three cycles of evidence: **Rigby A2 zoom-out sweeps for the drift-class or hardening-class of change being closed**. The pattern was recorded as "Fold C" in each session's handoff:

- **S3029 Fold C `1st trigger`** — DISCOVERY. Rigby's A2 sweep of `.update(status='canonical')` after PR #3746 landed discovered the 4th silent site in `core/tasks_ops.py:_impl_auto_approve_boardroom_items` that PR #3746 missed. Watch flagged for 2nd trigger.
- **S3030 Fold C `2nd cycle, no discovery`** — CLEAN. Rigby's A2 sweep ran 7 `repo_tool` searches: no other lingering `.update(status='canonical')` in production, no kwargs-shape variants, no adjacent `published/is_published` drift-class hits, no filter collisions. Recorded as "different signal from discovery — pattern working as designed, confidence bound: drift class genuinely closed."
- **S3031 Fold C `3rd cycle, no discovery`** — CLEAN. Rigby's A2 sweep ran 8 `repo_tool` searches: no other `promote_to_canonical` callers, no ungated `emit_canonical_promotion_broadcast` callers, no downstream `updated_at`-dependent consumers. S3031 close flagged the pattern as "arguably ready to codify with the 'clean-sweep-confirms-closure' framing rather than the original 'catches adjacent silent bugs' framing."

S3033 opened as a fresh terminal session per S3032 close's fresh-terminal recommendation (S3030+S3031+S3032 terminal had shipped 6 PRs; context density warranted fresh start). Two Lean options recommended at S3033 open: Lean 1 (`bulk_reject_decisions` broadcast symmetry, ~1 session) or Lean 2 (Fold C 3rd-cycle codification, ~30 min). Chris ratified Lean 2.

## §2. Ratified amendment scope

### §2.1 New rule PLAYBOOK-7.7.5 (class-scoped mandatory A2 sweep for drift/hardening intents)

Added under existing §7.7 Spec→Ship Workflow Shape (Chapter 7 stub-chapter partial-activation scope from v0.5.0; §7.7 first authored at v0.10.0 with rules 7.7.1–7.7.4).

**Rule text (as ratified — includes T1 SIGN `same_pr_mitigatable` refinements folded in at authoring):**

> **[GR] PLAYBOOK-7.7.5** When a spec→ship contract's T1 plan (per PLAYBOOK-7.7.1 Phase 2) declares its Substantive Intent as either (a) *drift-class closure* — restoring an invariant that a bug, race, or pattern has been silently violating — or (b) *hardening-class* — tightening a contract to eliminate a race, missing gate, or edge-case violation of an invariant — the Phase 2 plan MUST name the *shape signature* being closed: an exact query predicate, ORM filter, function name, decorator, or field invariant sufficient for a mechanical repo-wide sweep. The Phase 7 A2 SIGN routing MUST include a repo-wide zoom-out sweep enumerating, at minimum: (i) other production sites of the same shape signature (via grep/AST/ORM query for the exact shape named in the Phase 2 plan); (ii) adjacent classes with structurally similar risk, bounded to *same table/model OR same workflow stage OR same consumer boundary* — the reviewer MUST state inline which axis applies; (iii) downstream consumers of the changed invariant that would silently miss or misinterpret rows if the sweep leaves any site unclosed; (iv) tests that lock in the old behavior and would fail-in-reverse if the invariant is genuinely restored. The (i)–(iv) enumeration is a MINIMUM floor; additional sweep dimensions warranted by the specific class MAY be added. The sweep MUST be executed via `tool_runs` per PLAYBOOK-7.7.2 and its enumeration MUST appear inline in the A2 SIGN attestation. A "clean" sweep — zero findings across (i)–(iv) plus any added dimensions — satisfies the rule as tool-grounded evidence of class closure; a clean sweep is a first-class outcome, not a null result. A sweep that finds ≥1 previously-unknown site MUST fold back to Phase 6 for either same-PR incorporation (when the finding is within the current PR's scope per PLAYBOOK-6.10.8 `same_pr_actionable` / `same_pr_mitigatable`) OR forward-carry with a named trigger condition (`future_trigger`). This rule EXTENDS PLAYBOOK-7.7.2 (SIGN evidence discipline) with a class-scoped mandatory-sweep contract for the specific T1-declared intents of drift closure and hardening; it EXTENDS PLAYBOOK-6.10.7 (joint SIGN zoom-out ask) by promoting the zoom-out from open-ended prompt to enumerated-dimension sweep when the class-triggering conditions are met.

**Evidence citations (unchanged from draft):** E4 commits `d0841a0f6` (S3029) + `e7fc79282` (S3030) + `d7bb28b4f` (S3031); E6 handoffs SESSION_3029/3030/3031 §Fold C.

**Refinements folded in at authoring (Rigby T1 `same_pr_mitigatable`):**

1. **Shape signature naming requirement** — Phase 2 plan MUST name the exact query predicate / ORM filter / function name / decorator / field invariant being closed. Makes (i) grep-objective; prevents cargo-cult "must find something" or "the shape is fuzzy."
2. **Adjacent-class boundary** — (ii) bounded to *same table/model OR same workflow stage OR same consumer boundary*; reviewer states inline which axis applies. Prevents unbounded "search everything."
3. **"At minimum" prefix** — (i)–(iv) framed explicitly as MINIMUM floor, not exhaustive list. Allows future additions without spec-violation feel.
4. **"Clean sweep is a first-class outcome"** — explicit sentence added so future reviewers don't treat zero-findings as inadequate evidence.

### §2.2 Chapter 7 stub-chapter frontmatter refresh

Chapter 7 remains STUB with partial activation. Frontmatter updated:
- **Status:** appended "§7.7 extended at v0.11.0 with PLAYBOOK-7.7.5 (class-scoped mandatory A2 sweep for drift/hardening intents)"
- **Last substantive change:** v0.10.0 → v0.11.0
- **Rule ID range:** PLAYBOOK-7.1.1 through PLAYBOOK-7.7.5

### §2.3 §7.8 Cross-references extension (informative)

Added two informative cross-references:
- Chapter 6 §6.10 — PLAYBOOK-6.10.7 as parent contract that §7.7.5 promotes to enumerated-dimension sweep; PLAYBOOK-6.10.8 as fold classification/persistence mechanism invoked on fold-back.
- Handoffs SESSION_3029/3030/3031 §Fold C — three-cycle in-wild corroboration corpus.

### §2.4 Frontmatter updates

Playbook frontmatter fields updated:
- `version`: "0.10.0" → "0.11.0"
- `parent_version`: "0.9.0" → "0.10.0"
- `compatible_with`: appended "0.10.0"
- `ratified_date`: 2026-07-26 → 2026-07-28
- `git_tag`: playbook-v0.10.0 → playbook-v0.11.0
- `prior_ratification`: rolled forward from v0.9.0 → v0.10.0
- `authoring_sessions`: appended 3033
- `v0_11_0_authoring_session`: 3033 (new)
- `v0_11_0_ratification_session`: 3033 (new)
- `rule_count`: 211 → 212
- `rules_added_v0_11_0`: [PLAYBOOK-7.7.5] (new)

Body version header: `# Donkey Betz Engineering Playbook v0.10.0` → `v0.11.0`.

## §3. Three-cycle corroboration ledger

| Cycle | Session | PR | SHA | Sweep outcome | Handoff §Fold C classification |
|-------|---------|-----|-----|---------------|-------------------------------|
| 1st | S3029 | #3747 | `d0841a0f6` | DISCOVERY — 4th silent site in `core/tasks_ops.py:_impl_auto_approve_boardroom_items` caught, 4 bulk `.update(status='canonical')` calls | `1st trigger` — codify A2-zoom-out-sweep pattern (watch for 2nd) |
| 2nd | S3030 | #3749 | `e7fc79282` | CLEAN — 7 `repo_tool` searches: no lingering `.update(status='canonical')`, no kwargs-shape hiding, no adjacent `published/is_published` drift, no filter collision | `2nd cycle, no discovery` — pattern useful even when nothing found; watch for genuine 2nd-discovery to codify |
| 3rd | S3031 | #3751 | `d7bb28b4f` | CLEAN — 8 `repo_tool` searches: no other `promote_to_canonical` callers, no ungated broadcast callers, no `updated_at`-dependent consumers | `3rd cycle, no discovery` — arguably ready to codify with "clean-sweep-confirms-closure" framing |

Codification threshold judgment (per D2 Rigby T1 AGREE): the "clean-sweep-as-evidence" signal is a distinct codification pressure from "catches adjacent silent bug" — requiring only the latter would bias toward codifying gotchas that keep biting while missing rules whose value is *preventing misses*.

## §4. Rigby joint SIGN cycle

### §4.1 T1 dispatch (author-side directive)

Claude routed the T1 SIGN with:
- Amendment shape (v0.10.0 → v0.11.0 MINOR, insertion point, EXTENDS declarations).
- Full draft rule text.
- 3-cycle evidence corpus with PR # + SHA + handoff citations.
- 6 explicit `repo_tool` verify instructions (grep 3 handoffs for Fold C; read Playbook §7.7 body; grep for PLAYBOOK-6.10.7/6.10.8; read v0.9.0 envelope template).
- 5 AGREE/DISAGREE dimensions (D1 clarity, D2 evidence, D3 scope, D4 EXTENDS, D5 insertion+version).
- Open-ended zoom-out ask per PLAYBOOK-6.10.7 with 3 specific pressure-test framings (codify at 3 cycles too eager? enumeration over-specified? future rule interaction risk?).

### §4.2 T1 attestation (Rigby)

Rigby returned tool-grounded attestation. All 5 dimensions **AGREE**. Per-dimension tool_runs:
- **D1 clarity:** TR-4/5 anchored parent-rule framing. Ambiguity flagged at "adjacent classes" boundary — `same_pr_mitigatable` mitigation.
- **D2 evidence:** TR-1/2/3 confirmed 3-cycle handoff record. No fold; hedge appropriately captured.
- **D3 scope:** TR-4 confirmed §7.7 spec→ship scope; §7.7.5 stays inside contract.
- **D4 EXTENDS:** TR-5 confirmed 6.10.7/6.10.8 shapes; both EXTENDS hold, no SUPERSEDE risk.
- **D5 insertion+version:** TR-4 confirmed clean slot; v0.10.0→v0.11.0 MINOR consistent with prior cadence.

**Zoom-out fold — `same_pr_mitigatable`:** (i)–(iv) risks ritualization if shape isn't well-defined; adjacent-class subjective. Mitigation proposed: name shape signature in Phase 2 plan + bound adjacent-class axis + prefix enumeration "at minimum."

### §4.3 Same-PR mitigations applied at authoring

All four T1 refinements folded into the ratified rule text at §2.1 (shape-signature naming, adjacent-class boundary, "at minimum" prefix, clean-sweep-first-class explicit sentence). No T2 re-verification cycle needed — refinements were textual clarifications of the same normative content, not scope changes.

### §4.4 Ledger persistence (post-D-verdict backfill)

Zoom-out fold persisted to `logs/zoom_out_classifications.jsonl` via `record_zoom_out_concern` post-D-verdict. See §6 constitutional-debt note below for the procedural sequence.

## §5. Chris D-verdict

Presented via Phase 5 plain-English framing per PLAYBOOK-7.7.3:
- **What's in the amendment:** plain-English summary of the class-scoped A2 sweep rule.
- **Do we lose anything?** No — codifies existing practice; only fires on self-declared drift/hardening intent; adds Phase-2 shape-signature naming cost.
- **Is it more work later?** Yes if skipped — next drift/hardening arc re-derives sweep discipline; future author risk of skipping sweep and shipping partial fix (S3029 near-miss).
- **Version bump:** MINOR v0.10.0 → v0.11.0 (matches v0.6.0/v0.7.0/v0.8.0 single-rule cadence).
- **Rigby verdict summary:** AGREE all 5 dimensions + 1 `same_pr_mitigatable` already folded.

Chris D-verdict: **"ship it"** — RATIFIED 2026-07-28 S3033.

## §6. Constitutional debt disposition

**One procedural observation, no rule-level constitutional debt:**

The T1 zoom-out fold was classified `same_pr_mitigatable` and mitigated at §2.1 same-envelope, but the `record_zoom_out_concern` ledger-persistence write ran POST Chris D-verdict rather than before. PLAYBOOK-6.10.8 requires classify + persist before D-verdict is requested; the graceful-degradation clause permits deferral only for tool failure. This session did not experience a tool failure — the sequence gap was a process oversight during the tight T1→D-verdict window.

**Disposition:** procedural note recorded; ledger backfilled inside the ship bundle (same PR as this envelope). The precedent for backfill-inside-envelope was established at v0.9.0 (RATIFICATION_2026-07-22 §4.3 ledger backfill for S2885/S2886 corpus). No rule modification required; no CD entry opened. Watch for a 2nd trigger to determine whether a PLAYBOOK-6.10.8 sequencing sub-rule is warranted.

## §7. Post-ratification bindings (PLACEHOLDER fields — filled at merge / cascade)

- `parent_version_commit_sha` — filled at v0.10.0 head lookup post-merge
- `head_at_ratification` — filled at merge SHA
- `close_pr` — filled at v0.11.0 amendment PR number
- `cascade_pr` — filled at docs cascade PR number
- `cascade_pr_merge_sha` — filled at cascade merge SHA
- `workspace_ratification_deliverable_id` — filled at workspace deliverable mint post-merge

## §8. What this amendment teaches about how to do amendments

1. **Three cycles of a workflow-shape pattern is a valid codification threshold when at least one cycle is a discovery.** The v0.9.0 amendment codified test-authoring rules from two triggers; v0.11.0 codifies a session-shape rule from one discovery + two clean sweeps. Distinct evidence class: workflow-shape rules can be validated by "clean sweeps continuing to hold" in a way that content-shape rules cannot.
2. **`same_pr_mitigatable` folds at T1 can reshape rule text without a T2 SIGN cycle** when the refinements are textual clarifications of the same normative content rather than scope changes. The v0.11.0 T1 fold added specificity to (i)–(iv), a shape-signature requirement, and boundary language — none altered what the rule requires, just how a future reviewer identifies the trigger and executes the sweep.
3. **Ledger persistence timing matters for the constitutional record even when the D-verdict is uncontested.** PLAYBOOK-6.10.8's "persist before D-verdict" is a provenance-integrity rule: the ledger row should exist by the time Chris ratifies so the ratification act references a persisted classification, not a floating one. Backfill-inside-envelope preserves the record but changes the sequence; the graceful-degradation clause was written for tool failure, not for author oversight. Fold recorded in §6 as procedural note pending 2nd trigger.
