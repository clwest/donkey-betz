---
title: "Group 1800 Cat F — Adjacent / Separation Boundaries consolidation child audit (F.a external bridges + F.b cross-domain HAI-consumer gaps + F.c PA-tool learning intersection + F.d duplicate learning-service inventory + F.e terminology + arc evidence consolidation for xx99 §5 posture-decision brief)"
session: 1806
child_slot: P6
domain_slug: human_attention
research_group: 1800
category: child_audit
authority: child-audit for Category F per parent §5 D78 sequence + SIXTH AND LAST child under Group 1800; consolidates P1-P5 evidence into xx99 §5 posture-decision brief input
head_commit: eb6d5fcd
status: draft
date: 2026-07-04
last_verified: 2026-07-04
authors: Claude Code (Chris directed via short command "start research group 1806" — interpreted per playbook §21 short-command intent as S1806 child under Group 1800 D78 P6 slot; Rigby confirmed service_context: local on arc pin pa-ae5931ea706b4537 via platform_config_tool overview at session open)
prior_children:
  - 1801_human_attention_cat_a_human_attention_item_core_audit.md (S1801 Cat A HAI Core)
  - 1802_human_attention_cat_b_feedback_processor_child_audit.md (S1802 Cat B FeedbackProcessor)
  - 1803_human_attention_cat_c_learning_bridges_child_audit.md (S1803 Cat C Learning bridges)
  - 1804_human_attention_cat_d_human_preference_child_audit.md (S1804 Cat D HumanPreference)
  - 1805_human_attention_cat_e_verification_loop_child_audit.md (S1805 Cat E S746 verification loop)
parent: 1800_human_attention_domain_scoping.md
delegates_to:
  - xx99 S1899 canonical summary — consumes this doc's §14 F5 arc-close meta-methodology consolidation + §19 R0 posture-decision framing + §7 six-plane runtime map + Cat F sub-slot F.a-F.e verdicts
related_arcs:
  - 1300 Memory (AgentLearning + UserAgentLearning ownership; Cat F.d dedup verdict lands under Group 1300 territory)
  - 1400 Revenue (opportunity-outcome consumer gap; Cat F.b MISSING catalog inheritor)
  - 1500 Sports (SportsBettingLearningBridge caller of record_verification consumer; Cat F.d overlap with S1503)
  - 1600 Content (PALearningInsightsService origin — Session 605; Cat F.c inherits S1605 tactical-split boundary discipline)
  - 1700 Observability (failure-cluster → HAI MISSING catalog; Cat F.b inheritor)
  - 1900 Event Architecture (Group 1900 will inherit R0b source_kind enum + Cat F.b HAI-consumer bus candidates — parent §6.4 delegation)
playbook_application: §11.2 20-section child template ELEVENTH application overall + SIXTH AND LAST under Group 1800
verifier_loop: §14 verifier-loop REQUIRED CODIFICATION-READY (S1799 §10.2 MC-1) — pre-Explore + post-Explore + post-SIGN Q1 miss-vector rule-out grep performed; pre-Explore verifier CONFIRMED all four Cat F sub-slot entry-point classes exist at HEAD (RedditLearningBridge + BlueskyLearningBridge in ai_core/intelligence/; 4 duplicate learning services across 3 apps; PALearningInsightsService at core/services/); post-Explore verifier RESOLVED Agent 3 vs Agent 6 contradiction on PALearningInsightsService caller sites via direct grep (Agent 3 correct — ONE caller via lazy import at `core/services/pa_intelligence_enricher.py:544-559`; Agent 6 grep pattern missed lazy import); post-Explore verifier CONFIRMED duplicate-file class-name collisions at `core/services/boardroom_learning{,_service}.py:27,32` + `{core,ai_core/intelligence}/unified_learning_pipeline.py:9,54` (both same-class-name pairs — import-path-dependent binding); post-Explore verifier CORRECTED parent §5 F.e "six terms" to five semantic terms + verification-as-HAI-field per F.e.1 terminology matrix; **NO F5 correlation-primitive HYPOTHESIS box discipline SIXTH application at S1806** per S1805 close directive — F5 primitive-testing arc completes at S1805 for Group 1800 with running tally 1 pass / 4 disprove across 5 applications; S1806 CONSUMES the F5 arc via meta-methodology consolidation for xx99 §10 CODIFICATION-CONFIRMED promotion under utility-rate-vs-pass-rate framing
sign_status: SIGN-with-edits at High confidence 0.82 (2026-07-04) via Rigby SIGN cycle 1 single-batch 4-question on arc pin pa-ae5931ea706b4537 per S1801+S1802+S1803+S1804+S1805 arc-pin routing precedent (durable-by-fifth-application; S1806 CONFIRMS durable-by-sixth-application-across-six-child-arcs — established as arc-standard behavior); §1 biggest-risk risk-split paragraph fold (F7 strategic/systemic vs F3 immediate correctness) + F1 intentional-separation + dead-hook reframing fold + F2 verified-set + candidate-set + search-scope softening fold + F3 silent-import-collision mechanics explicitness fold + F4 no-canonical-writer-evidence + no-ADR-endorsement multiplicity-as-debt fold + F7 phantom-learning-loop 2-line tie-back fold + F.b maturity downgrade WORKING → PARTIAL (catalog works; integration doesn't) + F.d WORKING explicit-scoping-qualification fold + §17 canonical-plane one-paragraph statement fold + §19 R-slot next-step-gating line fold + §20.9 miss-vector rule-out grep extended (non-Learning-suffix shadow services + non-obvious PA-tool routes + additional cross-domain HAI-consumer gaps in other desks) landed pre-commit; D48 30th arm turn 1 CLEAN; 25th consecutive-fully-clean-arms sub-pattern CONFIRMED per single-batch 4-question criterion (S1799 §10.2 MC-2 CODIFICATION-READY promotion path continues advancing toward CODIFICATION-CONFIRMED at 25-consecutive-fully-clean-arms — S1806 close CONFIRMS the milestone)
companion_anchors:
  - docs/PLATFORM_INVENTORY.md
  - docs/PLATFORM_WHAT_IT_IS.md
  - docs/research/DOMAIN_RESEARCH_PLAYBOOK.md
  - docs/research/ARCHITECTURE_INDEX.md
  - docs/research/domains/human_attention/1800_human_attention_domain_scoping.md
  - docs/research/domains/human_attention/1801_human_attention_cat_a_human_attention_item_core_audit.md
  - docs/research/domains/human_attention/1802_human_attention_cat_b_feedback_processor_child_audit.md
  - docs/research/domains/human_attention/1803_human_attention_cat_c_learning_bridges_child_audit.md
  - docs/research/domains/human_attention/1804_human_attention_cat_d_human_preference_child_audit.md
  - docs/research/domains/human_attention/1805_human_attention_cat_e_verification_loop_child_audit.md
  - docs/research/platform_architecture_inventory.md
  - docs/research/platform/cross_domain_integration_audit.md
owner: claude
---

# Group 1800 Cat F — Adjacent / Separation Boundaries (Consolidation Child Audit)

## 1. Executive Summary

Cat F is the CONSOLIDATION surface: five sub-slots F.a–F.e (external LearningBridges + cross-domain HAI-consumer gaps + PA-tool learning intersection + duplicate learning-service inventory + terminology boundary) plus arc-close evidence consolidation for xx99 §5 D80 posture-decision brief. Cat F catalogs; per parent §3.F boundary rule + playbook §14.5 no-implementation rule, Cat F does NOT act on any F.a–F.e items. Post-arc T-slot inherits.

At HEAD `eb6d5fcd`, all four Cat F sub-slot entry-point classes verified at pre-Explore: `RedditLearningBridge` @ `ai_core/intelligence/reddit_learning_bridge.py:96` (998 LOC) + `BlueskyLearningBridge` @ `ai_core/intelligence/bluesky_learning_bridge.py:108` (938 LOC) — F.a. `AgentLearningService` @ `core/services/agent_learning_service.py:122` (795 LOC) + `AgentLearningSystem` @ `intelligence/agent_learning.py:22` (272 LOC) + `AgentLearningEngine` @ `ai_core/intelligence/agent_learning_engine.py:69` (766 LOC) + `PersistentLearningEngine` @ `ai_core/intelligence/persistent_learning_engine.py:42` (456 LOC) — F.d. `PALearningInsightsService` @ `core/services/pa_learning_insights.py:24` (447 LOC) — F.c. Cross-domain integration audit @ `docs/research/platform/cross_domain_integration_audit.md` §3.3 + §3.8 + §2 — F.b. Five semantic terms verified (feedback / learning / signal / preference / personalization) with "verification" as HAI schema field not standalone learning-signal primitive — F.e (parent §5 wording "six terms" CORRECTED to 5 semantic + 1 field-level).

**Headline verdicts:**

- **F1 (HIGH structural — Cat F.a) — External bridges (Reddit + Bluesky) are NOT `LearningBridge` ABC inheritors; they are on-demand intelligence aggregators, NOT autonomous learning writers. Boundary is INTENTIONAL SEPARATION, not accidental drift (Rigby SIGN cycle 1 fold clarification).** `ai_core/intelligence/reddit_learning_bridge.py:96` and `ai_core/intelligence/bluesky_learning_bridge.py:108` are STANDALONE classes with no inheritance clause. Contrast S1803 §3.2: all 9 CORE bridges in `core/learning_bridges/` inherit `LearningBridge` ABC @ `core/learning_bridges/base.py:13` and implement the `process_event(...)` contract with `.save()` / `.objects.create()` writes to `UserAgentLearning` @ 22+ sites. External bridges return in-memory dataclasses (`RedditInsight` / `SubredditTrend` / `CommunityConsensus` / `ExpertInsight` / `MarketSignal` / `TrendAnalysis`) via singleton getters `get_reddit_learning_bridge()` + `get_bluesky_learning_bridge()`; ZERO database writes; ZERO HAI plane interaction (grep of `HumanAttentionItem|HumanFeedbackRecord|HAI_item_id|record_verification|record_decision` across both files returns zero hits). Asymmetric consumption: RedditLearningBridge has 6 caller sites (`ai_core/intelligence/learning_loop.py:879, :1033, :1150` + `ai_core/spiders/advisor_feed.py:594` + `ai_core/agents/universal_agent_loader.py:598` + `ai_core/consciousness/unified_mind.py:144`); BlueskyLearningBridge has 1 registration site (`ai_core/consciousness/unified_mind.py:136`) with no observed method invocation at HEAD. Additionally, `async def start_continuous_learning()` @ `reddit_learning_bridge.py:197` + `bluesky_learning_bridge.py:151` defines 6-parallel-monitoring-task infrastructure but ZERO entry points invoke it (no `await bridge.start_continuous_learning()` grep hits; no beat schedule entry in `core/celery.py:37-796`). **Boundary rationale: DELIBERATE structural separation** (Rigby SIGN cycle 1 fold — treat as intentional separation absent evidence bridges were supposed to write to HAI/learning DB models; evidence supports "separation by design + some dead code" framing). `ai_core/intelligence/` is the external-signal aggregation zone; `core/learning_bridges/` is the internal-event learning-write plane. `start_continuous_learning()` @ `reddit_learning_bridge.py:197` + `bluesky_learning_bridge.py:151` should be labeled as **abandoned / dead integration hook** (not evidence the boundary "should" be integrated). Naming shared "LearningBridge" suffix creates false structural equivalence — recommend R3 rename. **Severity HIGH structural** (parent §2.4 + §5 D77 boundary rule was implicit; explicit codification required at xx99 §5). Cat F.a maturity: **PARTIAL-to-EXPERIMENTAL** (code compiles; on-demand fetch pattern LIVE for Reddit; `start_continuous_learning()` dead-code infrastructure).

- **F2 (HIGH consolidation — Cat F.b) — Cross-domain HAI-consumer gap catalog VERIFIED at 5 explicit MISSING integrations + 3 uncataloged candidates under scanned patterns; additional gaps likely in other desks (Rigby SIGN cycle 1 softening fold).** Parent §5 F.b "~5+ gap catalog" claim CONFIRMED at `docs/research/platform/cross_domain_integration_audit.md`: (1) Failure Cluster → HAI CRITICAL @ §3.3:438-447 (Observability domain; Body Coordinator @ `core/services/body_coordinator.py:130-181` defines 23 event handlers but ZERO emit HAI creation); (2) Signal Pattern → HAI MEDIUM @ §3.8:526-539 (`SignalCuratorService` @ `core/services/signal_curator_service.py:1-574` routes to AutoTopic but has no pattern-strength threshold gate for auto-HAI; no `create_signal_pattern_attention()` method in `human_attention_bridge.py`); (3) Body Systems → HAI MEDIUM @ §2.2:295 (`HeartBeat` @ `core/models_heart.py:25-97` accumulates without HAI consumer; all 23 BodyCoordinator handlers are internal side-effect-free); (4) Revenue Opportunity → HAI MEDIUM @ §2.4:317 (no `create_revenue_opportunity_attention()` method; `ImpactEvent` @ `tool_dispatcher.py:1010-1035` emitted but no HAI escalation); (5) Governance/KillSwitch → downstream MEDIUM @ §2.6:336 (WEAK read-only; HAI consumes governance state but does not gate mode changes). **Uncataloged candidates surfaced by S1806:** (6) Employee OS mission-failure → HAI (no `create_mission_escalation_attention()` at HEAD; `core/employees/mission_runner.py` writes OpsRunEvent but no HAI consumer registered); (7) Discord command failure → HAI (`core/services/discord_bot.py` 11,676-line monolith; grep finds zero HAI bridge calls from command handlers); (8) Sports/DBAO non-arbitrage outcomes → HAI (per S1805 F3, verification chain SPORTS-ARBITRAGE-ONLY; wagers + predictions have no auto-HAI on unexpected outcome). Positive contrast: `core/services/human_attention_bridge.py:44-637` has 10+ WORKING producer methods (`create_arbitrage_attention` @ :178, `create_pilot_gate_attention` @ :72, `create_agent_execution_attention` @ :121, `create_diagnostic_alert` @ :272, `create_content_review_attention` @ :386, `create_mythology_alert` @ :327, `create_system_alert` @ :229-266 unused). **Search-scope caveat (Rigby SIGN cycle 1 fold):** the F.b catalog was compiled by (a) reading cross_domain_integration_audit.md §2 + §3.3 + §3.8 exhaustively; (b) grepping producer domains (Observability, Signal Engine, Body Systems, Revenue Pipeline, Governance) for `create_.*_attention\|HumanAttentionBridge` writer calls; (c) sampling three uncataloged candidate domains (Employee OS, Discord, Sports non-arbitrage) for the same patterns. **Additional MISSING gaps likely exist in other desks not scanned** — e.g., governance/autopilot kill-switches (partial WEAK verified), messaging inbox/SLA, content-desk HAI producers. **Severity HIGH consolidation** (verified count is a lower bound). Cat F.b maturity: **PARTIAL — catalog works but integration doesn't** (Rigby SIGN cycle 1 downgrade from WORKING → PARTIAL: gaps are catalogued but enforcement/monitoring is absent; "catalog is working; integration is not"). Closure DEFERRED to xx99 §8 T-slot ADR.

- **F3 (HIGH — Cat F.d — IMMEDIATE correctness risk) — Silent import-path-dependent collision confirmed: `BoardroomLearningService` exists as SAME CLASS NAME in TWO distinct files under `core/services/`.** Direct grep at HEAD verified: `core/services/boardroom_learning.py:27` (`class BoardroomLearningService`) + `core/services/boardroom_learning_service.py:32` (`class BoardroomLearningService`). Similarly `UnifiedLearningPipeline` name-collision: `core/unified_learning_pipeline.py:9` + `ai_core/intelligence/unified_learning_pipeline.py:54`. **Silent import collision mechanics (Rigby SIGN cycle 1 fold explicitness):** Python module path — not class name — determines which class definition binds at call site. Callers doing `from core.services.boardroom_learning import BoardroomLearningService` vs `from core.services.boardroom_learning_service import BoardroomLearningService` resolve to STRUCTURALLY DIFFERENT class objects with DIFFERENT semantics (Session 602 weighted formula vs Session 940 PA feedback) despite identical class name. Type checkers + linters + tests referencing the class name do NOT catch the mismatch because both files declare `class BoardroomLearningService`. Refactoring an import statement in one caller silently rebinds it to the other class — behavior changes with no compile-time signal. S1803 Cat C §14 F4 surfaced these; S1806 CONFIRMS and elevates to arc-close finding. Neither pair has a documented deprecation ADR at HEAD. **Severity HIGH operational** (silent import ambiguity is a class of bug that survives typing + linting). Post-arc R-slot: consolidation ADR selecting the canonical file per pair + removal ADR for the losers. Cat F.d duplicate-file collision maturity: **WORKING-with-HIGH-consolidation-debt.**

- **F4 (HIGH — Cat F.d) — Four coexisting `Agent*Learning*` service classes verified; three of four are stale/dormant/fallback-only.** (a) `AgentLearningService` @ `core/services/agent_learning_service.py:122` (795 LOC) — **LIVE-USED** (13 caller sites: `core/agent_router.py` ×2 including `record_interaction` @ :169-173 + `get_adaptive_context` @ :186, `core/views_preferences.py` ×4, `core/views_image_gallery.py`, `core/tasks_agents.py`, `core/services/__init__.py` re-export); writes Redis only (no ORM writer); last touched 2026-04-15. (b) `AgentLearningSystem` @ `intelligence/agent_learning.py:22` (272 LOC) — **LIVE-USED-FALLBACK-ONLY** (single try/except site in `intelligence/agent_executor.py:1`); reads `AgentPerformanceMetrics` for sport-specific confidence adjustment. (c) `AgentLearningEngine` @ `ai_core/intelligence/agent_learning_engine.py:69` (766 LOC) — **DEFINED-BUT-UNSCHEDULED** (initialized in `core/command_center_ai.py` via `start_agent_learning()`; sets up Redis pubsub subscribing to `learning:signals:*` category channels; grep of `redis.*publish.*learning:signals` across `core/tasks*.py` returns zero publishers — listener loop blocks forever on empty channels). (d) `PersistentLearningEngine` @ `ai_core/intelligence/persistent_learning_engine.py:42` (456 LOC) — **DEFINED-BUT-UNUSED** (module-level singleton at `:456`; grep `from.*persistent_learning_engine import|import.*persistent_learning_engine` across `core/` + `intelligence/` returns ZERO production references; only docstring mention at `ai_core/intelligence/unified_learning_pipeline.py:3`). **None of the four services write to canonical `AgentLearning` or `UserAgentLearning` models — supports "shadow" interpretation (Rigby SIGN cycle 1 fold: no-canonical-writer-evidence).** Canonical human-mediated writer is `FeedbackProcessor` @ `core/models_feedback_processing.py` (S1802 §5); canonical autonomous writer plane is the 9 core `LearningBridge` subclasses (S1803 §3.2). **No ADR at HEAD endorses multiple canonical learning writers; therefore treat multiplicity as debt until proven intentional (Rigby SIGN cycle 1 fold).** Cat F.d matrix reveals **six-plane learning surface fragmentation** at HEAD: (i) FeedbackProcessor → AgentLearning + LearningInsight (human-vetted canonical); (ii) 9 LearningBridge subclasses → UserAgentLearning (autonomous canonical); (iii) AgentLearningService → Redis only (user-interaction ephemeral); (iv) AgentLearningEngine → Redis + in-memory (unscheduled); (v) PersistentLearningEngine → parallel model tree (`AgentLearningEvent` + `LearningDocument` + `LearningEmbedding` — but writer unreached); (vi) AgentLearningSystem → ORM READS only (no writes). **Severity HIGH architectural** (deferred to Chris-gate post-arc ADR). Cat F.d duplicate-service maturity: **WORKING-runs-without-immediate-errors (canonical writer plane operational; "WORKING" scoped to no runtime errors, NOT to coherent architecture — Rigby SIGN cycle 1 explicit-qualification fold) with HIGH consolidation debt (3 shadow services + 2 file-collision pairs).**

- **F5 (Cat F.c — LOW-MED) — PA-tool learning intersection BOUNDARY CLEAN per S1605 tactical-split precedent; ONE caller site with lazy-import invocation pattern.** `PALearningInsightsService` @ `core/services/pa_learning_insights.py:24` (447 LOC, Session 605) is a READ-ONLY context injector on `Experiment` + `ExperimentLearning` @ pilot-readiness domain (queries at `:197` filter + `:249-251` select_related join). ZERO WRITE operations. Caller inventory (post-Explore verifier grep): SINGLE call site @ `core/services/pa_intelligence_enricher.py:544-559` via LAZY import inside try/except (`from core.services.pa_learning_insights import PALearningInsightsService` @ `:550`; `service = PALearningInsightsService()` @ `:551`). No PA tool schema in `core/services/pa_tool_schemas.py` touches HAI/HumanFeedbackRecord/HumanPreference/AgentLearning/UserAgentLearning as writer. Adjacent tool inventory: `learning_patterns_tool` @ `pa_tool_schemas.py:379-403` reads `LearningPattern`; `feedback_tool` @ `:404-434` reads/writes `UserFeedback` (DISTINCT from HumanFeedbackRecord — verified naming, needs xx99 §5 anti-confusion note); `learning_tool` @ `:1999-2040` reads/writes `PAToolInsight` (PA-internal safety metadata; scope UNKNOWN pending post-arc verification); `remember_tool` @ `:2126-2177` writes `UserMemory`. **Boundary discipline verdict: CLEAN — PA tool surface (Rigby's surface) does NOT reach into HAI/Feedback/Preference/Learning writer plane.** Follows S1605 tactical-split precedent ("Cat E = PA tool surface contract; Cat D = canonical object model"). Two low-severity open questions: (a) `feedback_tool` handler `.save()` call scope at `core/services/td_handlers_content.py:3538` (list/stats vs actual write); (b) `PAToolInsight` model FK graph unknown (does it link back to HAI/AgentLearning?). **Severity LOW-MED architectural** (boundary is intact; open questions are governance-scope not blocking). Cat F.c maturity: **PARTIAL (service defined, single-caller path LIVE via lazy import; deployment status intact but topic-doc SILENT — `docs/topics/personal-assistant.md` enrichment table does NOT enumerate PALearningInsightsService).**

- **F6 (HIGH — Cat F.e) — Terminology is FIVE semantic terms plus VERIFICATION-as-HAI-field, not six standalone learning-signal primitives; boundary posture is PERMEABLE-BROKEN.** Parent §5 F.e "six overlapping-but-distinct terms" is CORRECTED at S1806 pre-Explore verifier: five semantic terms **feedback / learning / signal / preference / personalization** each have distinct owner surfaces + working definitions at HEAD; **verification** is a field-level property on `HumanAttentionItem` schema (`verification_outcome` + `verified_at` + `verification_profit` + `verification_notes` + `event_completed_at` @ `core/models_human_interface.py:155-163` per S1805 §3.1) invoked via `record_verification()` @ `:216-227`, not a standalone learning-signal primitive with its own model. F.e.1 terminology matrix maps each term to (a) owner surface (b) adjacent-term bleed (c) recommended xx99 posture: **feedback ↔ learning PERMEABLE** (post_save signal coupling clean); **learning ↔ personalization PERMEABLE-BROKEN** per S1804 F4 signal chain broken; **preference ↔ personalization PERMEABLE-BROKEN** per S1801 F2 producer-side enforcement absent; **verification-field ↔ learning PERMEABLE-BROKEN** per S1805 F4 post_save signal absent; **signal ↔ everything PERMEABLE** (fragmentation is operational not terminology-level). **Cross-cutting recommendation for xx99 §5:** REIFY-DISTINCT via `source_kind` enum on learning rows (S1803 R8 candidate; adds provenance tagging without breaking permeable couplings). **Severity HIGH consolidation** (terminology boundary directly supports D80 axis evidence). Cat F.e maturity: **EXPERIMENTAL (research foundational but boundary codification INCOMPLETE; xx99 posture-decision must frame post-arc terminology ADR).**

- **F7 (CRITICAL STRATEGIC — durable-at-three consolidation) — Compound learning-loop false-confidence pattern durable across S1804 F4 + S1805 F4 + S1801 D5 inherited: four break-points in ONE loop; blocks D80 posture-decision; creates phantom learning loop (Rigby SIGN cycle 1 phantom-loop tie-back fold: blocks single query answering "what did we learn about Agent X across all signal types?" + creates phantom round-trip where code paths exist at all four layers but data never flows).** (a) S1804 F4: `HumanFeedbackRecord.post_save` receiver @ `core/models_feedback_processing.py:372-392` does NOT call `HumanPreference.update_learned_stats()`; auto-approve path @ `core/services/human_attention_lifecycle.py:325` bypasses `record_decision()` entirely — **signal chain BROKEN at FeedbackProcessor layer.** (b) S1805 F4: `record_verification()` @ `core/models_human_interface.py:216-227` emits NO signal / NO event / NO log; SportsBettingLearningBridge coupling lives ONLY in `BettingOutcomeVerifier._create_learning_records()` @ `core/services/betting_outcome_verifier.py:473-476`, not in post_save receiver; REST-endpoint verifications @ `core/views_human_interface.py:245` NEVER trigger the bridge — **coupling FRAGILE at writer layer.** (c) S1804 F1: `HumanPreference.update_learned_stats()` NEVER computes `topic_weights` (never anywhere in codebase) + explicitly EXCLUDES `source_weights` from `save(update_fields=[...])` @ `core/models_human_interface.py:356-358`; `source_weights` mutated @ `core/services/human_interface_service.py:735-738` immediately discarded — **persistence BROKEN at model layer.** (d) S1801 D5 inherited durable-at-two: writer surfaces (`record_decision` + `record_verification`) emit no learning-loop-relevant signals; both use plain `self.save()` with no post_save receivers filtered on `STATUS_VERIFIED` or `decision='watch'`. **Compound consequence:** the "only round-trip w/ learning" per S1274 §4.7 canonical narrative is a phantom round-trip — code path exists at all four layers, data flow does not. Auto-approve decisions skip preference updates. Manual REST verifications skip learning-bridge. FeedbackProcessor runs but preference stays stale. Three separate break-points in ONE loop. **Severity CRITICAL architectural** (blocks D80 posture-decision; violates S1274 §4.7 canonical narrative). Consolidation impact: **xx99 §5 posture-decision brief cannot cleanly assess Option A vs B vs C vs D until coupling is wired.** xx99 should frame D80 selection as "with-assumptions-pending-fix" or defer to post-T-slot per playbook §14.5.

- **F8 (MED — durable-at-three consolidation) — Producer-side personalization enforcement absent across S1801 F2 + S1804 F4 + S1806 Cat F.b sample.** Zero of 41+ HAI producers @ `core/services/human_attention_bridge.py:80-150` check `HumanPreference.blocked_sources` / `min_urgency_to_notify` / `trusted_agents` / `require_review_above_confidence` before creating HAI rows. Governance gate applies ONLY at CONSUMER side (queue display filter @ `core/services/human_attention_lifecycle.py:248-249, :272` + priority scoring @ `core/services/human_interface_service.py:717-720`), not at CREATION time. Extended across Cat F.b: none of the 5 verified MISSING integrations (Failure Cluster / Signal Pattern / Body Systems / Revenue Opportunity / Governance KillSwitch) would honor producer-side personalization even if the integrations were wired — the enforcement primitive doesn't exist. **Runtime consequence:** user sets `blocked_sources=['spider_action']`; spider_action_pipeline continues creating HAI rows for that user; user must manually filter on UI. Governance gate is post-hoc (filter after creation), not preventive (prevent creation in first place). **Severity MED architectural** (blocks D80 axis producer-side evidence gathering). Cat F extends S1801 F2 + S1804 F4 pattern to consolidation-scope durable-at-three under Group 1800.

- **F9 (LOW-MED — durable-at-six consolidation) — Zero test coverage on Cat F sub-slot surfaces; test-gap pattern durable across ALL six children under Group 1800.** No test file matches `test_reddit_learning|test_bluesky_learning|test_pa_learning_insights|test_agent_learning_service|test_boardroom_learning|test_persistent_learning|test_agent_learning_engine|test_agent_learning_system|test_unified_learning_pipeline` under `core/tests/*.py` (Explore Agent 6 verified via grep). S1801 F9 + S1802 F9 + S1803 F9 + S1804 F9 + S1805 F9 + S1806 F9 durable-at-SIX under Group 1800 (test-gap pattern arc-wide CONFIRMED per playbook §20 two-triggers rule). **Runtime consequence:** future changes to any Cat F surface land without regression coverage. Cat F xx99 §10 candidate for meta-methodology codification: "durable-at-N test-gap is arc-close diagnostic; N=6-consecutive-children under one arc promotes to arc-wide TEST-GAP-CONFIRMED verdict." xx99 §8 T-slot inheritance follows S1801-S1805 pattern.

- **F10 (LOW — durable-at-six consolidation) — Zero admin registration + no coverage-query surface for Cat F sub-slot surfaces; admin-gap pattern durable-at-six.** Django admin @ `core/admin.py` (S1805 §3 Agent 6 verified) does not register any of the four F.d duplicate services, PALearningInsightsService, or the external bridges (services do not register in admin normally; the finding is scoped to admin surface for models these services touch). No dedicated coverage-query dashboard reports (a) which HAI producers respect vs bypass personalization, (b) which cross-domain integrations are wired vs missing, (c) which learning services are LIVE vs dormant. Extends S1801 F10 + S1802 F10 + S1803 F10 + S1804 F10 + S1805 F10 durable-at-SIX under Group 1800. **Severity LOW consolidation.** xx99 §8 T-slot inheritance follows.

**Biggest architectural risk — RISK-SPLIT FRAMING (Rigby SIGN cycle 1 fold):** Present two distinct risks so reviewers don't argue past each other over "biggest risk" depending on horizon:

- **F7 = highest STRATEGIC / SYSTEMIC risk (blocks D80 posture-decision).** Compound learning-loop false-confidence pattern with four break-points in the "only round-trip w/ learning" phantom loop; xx99 §5 posture-decision cannot cleanly select Option A/B/C/D until coupling is wired.
- **F3 = highest IMMEDIATE CORRECTNESS risk (could break quietly tomorrow).** Silent import-path-dependent collision on two duplicate `BoardroomLearningService` files + two duplicate `UnifiedLearningPipeline` files; refactoring an import statement silently rebinds to a structurally different class with different semantics; no compile-time signal.

**Canonical-plane statement (Rigby SIGN cycle 1 fold — essential for xx99 §5 D80 consumers to reason "what exists" vs "what's aspirational"):** At HEAD `eb6d5fcd`, de facto canonical learning writers appear to be `FeedbackProcessor` (human-mediated writer to AgentLearning + LearningInsight) + 9 core `LearningBridge` subclasses (autonomous writer to UserAgentLearning). The four Cat F.d shadow services (`AgentLearningService` LIVE-USED-Redis-only + `AgentLearningSystem` FALLBACK-ONLY-ORM-read + `AgentLearningEngine` DEFINED-BUT-UNSCHEDULED + `PersistentLearningEngine` DEFINED-BUT-UNUSED) appear to be partial/legacy/experimental alternatives to the canonical plane, not intentional specialization. No ADR endorsement documented.

**The Group 1800 learning surface is architecturally fragmented across SIX planes (FeedbackProcessor canonical human-mediated + 9 core LearningBridges canonical autonomous + 2 external ai_core/intelligence intelligence-aggregators + AgentLearningService Redis-ephemeral + AgentLearningEngine unscheduled-async + PersistentLearningEngine unused-database-tree) with NO unified event schema, NO source_kind provenance tag, TWO duplicate-file class-name collisions, and FOUR break-points in the "only round-trip w/ learning" that render the round-trip a phantom.** The system appears to have canonical unification (single AgentLearning + UserAgentLearning writer plane) at schema level, but has silently accreted five shadow paths that each represent an incomplete or abandoned attempt at supplementing the canonical writer. The result is not architectural chaos (each shadow was scoped to a specific concern) but architectural exhaustion — no single query answers "what did we learn about Agent X across all signal types?" because the answer is spread across six schemas with no unification pointer. This is the S1804 §1 "non-functional personalization loop that appears functional" pattern promoted to arc-wide FRAGMENTED-LEARNING-SURFACE at Cat F consolidation. **The D80 posture-decision brief inherits this framing: no option (A/B/C/D) can be cleanly selected until (a) F.d dedup ADR lands, (b) F5 loop-coupling wires the four break-points, (c) source_kind enum makes the human-mediated vs autonomous split explicit.**

**Biggest gaps for future research:** (a) **R0 (SYSTEMIC POST-ARC — D80 posture-decision framing update)** — parent §2.5 D80 four-option framing (Option A Canonical Unification / Option B Structural Separability / Option C Hybrid / Option D Shared surface) inherits F7 CRITICAL constraint that all four options are blocked at HEAD by learning-loop coupling breaks; xx99 §5 should re-frame D80 selection as "with-assumptions-pending-fix" OR defer post-T-slot; R0 is the arc-wide framing update that gates all subsequent R-slot ordering; (b) **R1 F.d duplicate learning-service consolidation ADR** (which of 4 services survives; deprecate 3; hard-delete PersistentLearningEngine per S1244 dormant cleanup pattern; consolidate BoardroomLearningService + UnifiedLearningPipeline name-collision pairs); (c) **R2 F.b cross-domain HAI-consumer integration closure ADR** (5 explicit MISSING + 3 uncataloged candidates; per-domain consumer contracts + threshold-based auto-escalation); (d) **R3 F.a external-bridge naming clarity ADR** (rename `RedditLearningBridge`/`BlueskyLearningBridge` to `RedditIntelligenceBridge`/`BlueskyIntelligenceBridge` to distinguish from `core/learning_bridges/` writer plane; delete `start_continuous_learning()` dead code); (e) **R4 F.c PALearningInsightsService topic-doc update** (add to `docs/topics/personal-assistant.md` enrichment table; document lazy-import invocation vector); (f) **R5 F.e source_kind enum schema-change ADR** (S1803 R8 candidate; provenance tagging on AgentLearning + UserAgentLearning rows to distinguish human-mediated / autonomous / verification-outcome / external-signal sources); (g) **R6 F.e terminology glossary ADR** (5-semantic-term matrix + verification-as-HAI-field codification; xx99 §5 posture-decision brief consumer); (h) **R7 loop-coupling repair ADR bundle** (S1804 F4 + S1805 F4 + S1801 D5 durable-at-two → durable-at-three consolidation; post_save receivers on `HumanFeedbackRecord` + `HumanAttentionItem` STATUS_VERIFIED; loop test coverage); (i) **R8 producer-side personalization enforcement ADR** (S1801 F2 + S1804 F4 + F.b sample durable-at-three; `HumanAttentionBridge.create_item()` should filter against `HumanPreference.blocked_sources` + `min_urgency_to_notify` before emit); (j) **R9 F.c PAToolInsight scope investigation** + `feedback_tool` `.save()` call scope investigation at `core/services/td_handlers_content.py:3538`; (k) **R10 F9 durable-at-six test-gap arc-close ADR bundle** (unified test-suite scope + regression harness for Cat F surfaces); (l) **R11 F10 durable-at-six coverage-query dashboard** (personalization-respect + cross-domain-integration + service-liveness observability).

Runtime maturity classification arc-wide: **PARTIAL-to-WORKING with EXPERIMENTAL consolidation.** F.a EXPERIMENTAL / F.b WORKING-gaps-cataloged-closure-deferred / F.c PARTIAL / F.d WORKING-canonical-writer-plane-plus-HIGH-shadow-debt / F.e EXPERIMENTAL. No arc-close STABLE rating possible without F.d dedup + F7 loop-coupling + F.e terminology codification.

## 2. Domain Purpose

**Q1 What is the Cat F "Adjacent / Separation Boundaries" consolidation surface and what is it responsible for?** Cat F is the arc-close consolidation surface for Group 1800 (HumanAttention / Feedback / Learning). Its purpose is TWO-fold per parent §5 P6 rationale: (a) audit five sub-slots F.a–F.e that are HumanAttention/Feedback/Learning-adjacent but NOT part of Cat A-E core surface — external LearningBridges + cross-domain HAI-consumer gaps + PA-tool learning intersection + duplicate learning-service inventory + terminology boundary; (b) consolidate P1-P5 (S1801-S1805) child audit evidence into an evidence brief for the xx99 S1899 canonical summary §5 D80 four-option posture-decision brief. Cat F is a CONSOLIDATION audit, not a direct architecture audit — it inherits boundary discipline "catalogs; does NOT act on any of F.a-F.e items" per parent §3.F. All F1-Fn findings promote to post-arc T-slot; no Cat F-scope implementation lands under S1806 per playbook §14.5 no-implementation rule.

**Q2 What are the biggest gaps in this domain today?** The most load-bearing gap is F7 CRITICAL compound learning-loop false-confidence pattern (four break-points in ONE loop; durable across S1801 D5 + S1804 F4 + S1805 F4 + S1806 arc-close consolidation). Additional gaps: F3 HIGH duplicate-FILE class-name collisions (`BoardroomLearningService` + `UnifiedLearningPipeline` pairs); F4 HIGH four-service coexistence with 3-of-4 stale/dormant/fallback-only; F1 HIGH structural — external `Reddit`/`BlueskyLearningBridge` are NOT `LearningBridge` inheritors + name creates false structural equivalence; F2 HIGH consolidation — 5 explicit MISSING cross-domain HAI-consumer integrations + 3 uncataloged candidates; F6 HIGH consolidation — terminology 5-semantic-terms + verification-as-HAI-field with PERMEABLE-BROKEN boundary posture; F8 MED durable-at-three producer-side personalization enforcement absence; F9 + F10 LOW-MED durable-at-six test-gap + admin-gap patterns. Arc-wide xx99 §5 posture-decision blocked at HEAD by (a) F.d dedup ADR pending, (b) F7 loop-coupling wire-up pending, (c) F.e source_kind enum codification pending.

## 3. Canonical Entry Points

### 3.1 Sub-slot F.a — External-domain LearningBridges (`ai_core/intelligence/`)

- **`RedditLearningBridge`** @ `ai_core/intelligence/reddit_learning_bridge.py:96` — 998 LOC. Standalone class (no `LearningBridge` ABC inheritance). Singleton getter `get_reddit_learning_bridge()`. Public methods: `get_insights_for_advisor(advisor_name, keywords)`, `get_trending_topics()`, `get_community_consensus(topic)`. Returns dataclasses: `RedditInsight` / `SubredditTrend` / `CommunityConsensus`. `async def start_continuous_learning()` @ `:197-216` defines 6 parallel monitoring tasks — DEAD CODE (no invocation site at HEAD).
- **`BlueskyLearningBridge`** @ `ai_core/intelligence/bluesky_learning_bridge.py:108` — 938 LOC. Standalone class. Singleton getter `get_bluesky_learning_bridge()`. Returns dataclasses: `ExpertInsight` / `MarketSignal` / `TrendAnalysis`. `async def start_continuous_learning()` @ `:151-167` defines 6 parallel learning tasks — DEAD CODE.
- **Base contrast:** `core/learning_bridges/base.py:13` (`LearningBridge` ABC) — NEITHER external bridge inherits.

### 3.2 Sub-slot F.b — Cross-domain HAI-consumer gap catalog

Five explicit MISSING entries verified at `docs/research/platform/cross_domain_integration_audit.md`:

- **Failure Cluster → HAI** MISSING CRITICAL @ §3.3:438-447. Producer domain: Observability. `core/services/body_coordinator.py:130-181` has 23 handlers; ZERO emit HAI. `core/services/human_attention_bridge.py:44-637` has no `create_failure_cluster_attention()` method.
- **Signal Pattern → HAI** MISSING MEDIUM @ §3.8:526-539. Producer: Signal Engine. `core/services/signal_curator_service.py:1-574` routes to AutoTopic but has no pattern-strength → HAI threshold gate. No `create_signal_pattern_attention()` method.
- **Body Systems → HAI** MISSING MEDIUM @ §2.2:295. `core/models_heart.py:25-97` accumulates HeartBeat; no consumer. BodyCoordinator handlers all side-effect-free.
- **Revenue Opportunity → HAI** MISSING MEDIUM @ §2.4:317. `core/services/tool_dispatcher.py:1010-1035` emits `ImpactEvent`; no HAI escalation. No `create_revenue_opportunity_attention()` method.
- **Governance / KillSwitch → HAI** WEAK MEDIUM @ §2.6:336. HAI consumes governance state read-only; does not gate governance mode changes.

Positive contrast — 10+ WORKING producer methods on `core/services/human_attention_bridge.py`:
- `create_pilot_gate_attention` @ `:72`
- `create_agent_execution_attention` @ `:121`
- `create_arbitrage_attention` @ `:178`
- `create_system_alert` @ `:229-266`
- `create_diagnostic_alert` @ `:272-321`
- `create_mythology_alert` @ `:327`
- `create_content_review_attention` @ `:386`

Three uncataloged candidate gaps surfaced by S1806:
- Employee OS mission-failure escalation (no `create_mission_escalation_attention()`)
- Discord command-failure escalation (`core/services/discord_bot.py` monolith; zero HAI bridge calls from command handlers)
- Sports/DBAO non-arbitrage outcomes (verification chain SPORTS-ARBITRAGE-ONLY per S1805 F3)

### 3.3 Sub-slot F.c — PA-tool learning intersection

- **`PALearningInsightsService`** @ `core/services/pa_learning_insights.py:24` — 447 LOC. Standalone (no inheritance). Session 605 docstring. Public methods 8 + private 6. READ-ONLY on `Experiment` model @ `:197` (`.objects.filter`) + `ExperimentLearning` @ `:249-251` (`.objects.filter().select_related('learning')`). ZERO writes anywhere. Module-level singleton @ `:445`.
- **Caller (LIVE, single site via lazy import):** `core/services/pa_intelligence_enricher.py:544-559` — `from core.services.pa_learning_insights import PALearningInsightsService` @ `:550`; `service = PALearningInsightsService()` @ `:551`; exception-guarded at `:559` (`self.logger.warning(f"Could not import PALearningInsightsService: {e}")`). This is the SINGLE production invocation vector at HEAD.

Adjacent PA-tool learning-plane surfaces (per `core/services/pa_tool_schemas.py`):
- `learning_patterns_tool` @ `:379-403` — reads `LearningPattern` object-level via `.objects.filter()` in handler @ `core/services/td_handlers_content.py:3314` (lines 3340, 3386, 3405).
- `feedback_tool` @ `:404-434` — reads/writes `UserFeedback` (DISTINCT from `HumanFeedbackRecord` — verified naming); handler @ `td_handlers_content.py:3436` with `.save()` call @ `:3538` (scope pending post-arc clarification).
- `learning_tool` @ `:1999-2040` — reads/writes `PAToolInsight` (PA-internal safety metadata); handler @ `td_handlers_core.py:1896` with `.update()` at `:1936-1947`.
- `remember_tool` @ `:2126-2177` — writes `UserMemory`; handler @ `td_handlers_core.py:2192`.

None of the four PA tools reach into HAI / HumanFeedbackRecord / HumanPreference / AgentLearning / UserAgentLearning writer plane. Boundary CLEAN per S1605.

### 3.4 Sub-slot F.d — Duplicate learning-service inventory (canonical + shadow)

**Canonical writer plane (for reference; NOT audited here — see S1802 §5 + S1803 §3.2):**
- `FeedbackProcessor` @ `core/models_feedback_processing.py:33-330` — canonical human-mediated writer to `AgentLearning` + `LearningInsight`.
- 9 `LearningBridge` subclasses @ `core/learning_bridges/*.py` — canonical autonomous writer to `UserAgentLearning` (22+ sites).

**Shadow/coexisting services (F.d audit scope):**
- `AgentLearningService` @ `core/services/agent_learning_service.py:122` — 795 LOC, standalone. Public methods 13 (`record_interaction`, `get_user_memory`, `save_memory`, `get_top_preferences`, `get_adaptive_context`, `apply_preferences_to_params`, `share_learning_as_knowledge`, `get_learning_stats`, `get_user_preferences_summary`, `clear_user_preferences`, etc.) + 6 module-level convenience functions. **LIVE-USED**. 13 caller sites: `core/agent_router.py` @ `:169-173` (`record_interaction`) + `:186` (`get_adaptive_context`); `core/views_preferences.py` ×4; `core/views_image_gallery.py`; `core/tasks_agents.py`; `core/services/__init__.py` re-export. Persists to Redis only. Last touched 2026-04-15.
- `AgentLearningSystem` @ `intelligence/agent_learning.py:22` — 272 LOC, standalone. Public methods 5 (`get_confidence_adjustment`, `should_make_prediction`, `get_specializations`, `get_confidence_calibration`, `_generate_performance_summary`). Reads `AgentPerformanceMetrics` ORM. **LIVE-USED-FALLBACK-ONLY**. Single try/except invocation @ `intelligence/agent_executor.py:1` during agent initialization. Last touched 2026-06-27.
- `AgentLearningEngine` @ `ai_core/intelligence/agent_learning_engine.py:69` — 766 LOC, standalone. Public async methods 5 (`initialize`, `get_agent_learning_status`, `get_learning_engine_status`, `enhance_agent_with_learned_knowledge`, `cleanup`). Initialized via `start_agent_learning()` singleton factory. **DEFINED-BUT-UNSCHEDULED**. Called at `core/command_center_ai.py` startup; sets up Redis pubsub subscription to `learning:signals:*` channels @ `:202-221`; NO PUBLISHER exists in `core/tasks*.py` or beat schedule. Listener loop @ `:282` (`_process_learning_signals`) would block forever. Last touched 2026-06-27.
- `PersistentLearningEngine` @ `ai_core/intelligence/persistent_learning_engine.py:42` — 456 LOC, standalone. Module-level singleton @ `:456`. Public async methods 7 (`start_learning_session`, `process_learning_signal`, `batch_process_learning_signals`, `get_agent_learning_status`, `end_learning_session`, `get_system_stats`, `cleanup`). Writes `AgentLearningEvent` + `LearningDocument` + `LearningEmbedding` + `AgentKnowledgeBase` + `AgentLearningSession` models. **DEFINED-BUT-UNUSED**. Grep `from.*persistent_learning_engine import|import.*persistent_learning_engine` across `core/` + `intelligence/` returns ZERO production references. Only docstring mention @ `ai_core/intelligence/unified_learning_pipeline.py:3`. Last touched 2026-06-27.

**Duplicate-file class-name collision pairs (S1803 F4 CONFIRMED + elevated):**
- `BoardroomLearningService` @ `core/services/boardroom_learning.py:27` (449 LOC, Session 602 weighted formula variant, 3 callers) vs `core/services/boardroom_learning_service.py:32` (420 LOC, Session 940 PA feedback variant, 4 callers) — **SAME class name, distinct purposes, both alive**.
- `UnifiedLearningPipeline` @ `core/unified_learning_pipeline.py:9` (55 LOC, likely legacy) vs `ai_core/intelligence/unified_learning_pipeline.py:54` (609 LOC, likely active) — **SAME class name, 11:1 LOC ratio**.

### 3.5 Sub-slot F.e — Terminology boundary

Five semantic terms + verification-as-HAI-field verified at HEAD:

- **feedback** — human's recorded judgment on HAI decision. Owner: `HumanFeedbackRecord` model + `FeedbackProcessor` @ `core/models_feedback_processing.py:33-330`. Classifier @ `:156-157`.
- **learning** — computed state update in Agent/UserAgentLearning/AgentMemory models. Owner: FeedbackProcessor writer (human-vetted) + 9 LearningBridges + 4 non-bridge writers (autonomous). Two production writer planes: `AgentLearning` (human-mediated canonical) vs `UserAgentLearning` (autonomous canonical).
- **signal** — domain event triggering learning or decision routing. Six subtypes at HEAD: human-decision signals (HumanFeedbackRecord.post_save), autonomous outcome signals (9 LearningBridge receivers), verification signals (S746 record_verification), personalization-read signals (HumanPreference reads), external social signals (Reddit/Bluesky bridges), cross-domain reads (HAI-consumer integrations). No centralized signal schema.
- **preference** — per-user configuration + learned-statistics on `HumanPreference` @ `core/models_human_interface.py:268-358`. Three field planes: governance (WORKING), learning (BROKEN per S1804 F1), notification (DEAD per S1804 F6).
- **personalization** — application of learned preferences at real-time decision gating. Three manifestations: governance personalization (WORKING), ranking personalization (BROKEN), preference-respect (NOT IMPLEMENTED). Owner: `HumanInterfaceService` + `HumanAttentionLifecycleService`.
- **verification** — post-event outcome-determination mechanism on HAI schema (`verification_outcome` + `verified_at` + `verification_profit` + `verification_notes` + `event_completed_at` fields @ `core/models_human_interface.py:155-163`; writer `record_verification()` @ `:216-227` — per S1805 §3.1-3.2). NOT a standalone learning-signal primitive with its own model. Sports-arbitrage-only scope per S1805 F3.

**Parent §5 F.e "six overlapping-but-distinct terms" CORRECTED at S1806 pre-Explore verifier: 5 semantic terms + verification-as-HAI-field.**

## 4. Major Models

Cat F is CONSOLIDATION scope. This section catalogs models touched by Cat F sub-slots F.a-F.e; no new models are introduced. Full model provenance/lineage was audited across S1801 (HAI + producers), S1802 (HumanFeedbackRecord + FeedbackProcessor), S1803 (UserAgentLearning + LearningInsight bridges), S1804 (HumanPreference), and S1805 (HAI verification tier).

| Model | File:Line | Cat F sub-slot | Read/Write scope in F | Ownership |
|-------|-----------|----------------|----------------------|-----------|
| `HumanAttentionItem` | `core/models_human_interface.py:36-227` | F.b (missing producer gaps) + F.c (PA-tool boundary) | NEVER touched by any F sub-slot service | Group 1800 Cat A (S1801) |
| `HumanFeedbackRecord` | `core/models_human_interface.py:230-265` | F.c (PA-tool boundary) | NEVER touched by any F sub-slot service | Group 1800 Cat B (S1802) |
| `HumanPreference` | `core/models_human_interface.py:268-358` | F.c (PA-tool boundary) + F.e terminology | NEVER touched by any F sub-slot service | Group 1800 Cat D (S1804) |
| `AgentLearning` | `core/models/ai_learning/models.py` | F.d canonical reference | NEVER touched by any of 4 shadow services | Group 1300 Memory + Cat B FeedbackProcessor writer (canonical) |
| `UserAgentLearning` | `core/models/ai_learning/models.py` | F.d canonical reference | NEVER touched by any of 4 shadow services | Group 1300 Memory + Cat C LearningBridges writer (canonical) |
| `LearningInsight` | `core/models/ai_learning/models.py` | F.d canonical reference | NEVER touched by any of 4 shadow services | Group 1300 Memory + Cat B FeedbackProcessor writer (canonical) |
| `AgentMemory` | `core/models/ai_learning/models.py` | F.d shadow | `AgentLearningService` READ/WRITE (in-memory) | Group 1300 Memory |
| `AgentKnowledgeSource` | `core/models/ai_learning/models.py` | (out of scope for F) | NONE | Group 1300 Memory |
| `Experiment` | `core/models_pilot_readiness.py` | F.c (PALearningInsightsService READ) | READ @ `pa_learning_insights.py:197` | Pilot-readiness domain |
| `ExperimentLearning` | `core/models_pilot_readiness.py` | F.c (PALearningInsightsService READ) | READ @ `pa_learning_insights.py:249-251` (select_related join) | Pilot-readiness domain |
| `LearningPattern` | `core/models_learning_patterns.py` (or similar) | F.c (learning_patterns_tool READ) | READ in `td_handlers_content.py:3340, 3386, 3405` | PA-tool domain |
| `UserFeedback` | `core/models_user_feedback.py` (or similar; DISTINCT from HumanFeedbackRecord) | F.c (feedback_tool READ/WRITE) | READ + potential WRITE @ `td_handlers_content.py:3538` (scope pending) | PA-tool domain |
| `PAToolInsight` | `core/models_pa_tools.py` (or similar) | F.c (learning_tool READ/WRITE) | READ + WRITE (`.update(safety_class=...)`) @ `td_handlers_core.py:1936-1947` | PA-tool domain |
| `UserMemory` | `core/models_pa_memory.py` (or similar) | F.c (remember_tool WRITE) | WRITE via `.create()` in `td_handlers_core.py:2192` handler | PA-tool domain |
| `AgentLearningEvent` | `ai_core/intelligence/models.py` | F.d (PersistentLearningEngine target) | UNWRITTEN (engine unused) | Shadow — no active writer |
| `LearningDocument` | `ai_core/intelligence/models.py` | F.d (PersistentLearningEngine target) | UNWRITTEN | Shadow |
| `LearningEmbedding` | `ai_core/intelligence/models.py` | F.d (PersistentLearningEngine target) | UNWRITTEN | Shadow |
| `AgentKnowledgeBase` | `ai_core/intelligence/models.py` | F.d (PersistentLearningEngine target) | UNWRITTEN | Shadow |
| `AgentLearningSession` | `ai_core/intelligence/models.py:211` | F.d (S1803 F3 drift finding — `ai_intelligence` variant survives; `core` variant deleted per migration `368_session_1244_delete_agentlearningsession.py`) | UNWRITTEN in `ai_intelligence` app; deleted in `core` | Shadow with migration drift |
| `HeartBeat` | `core/models_heart.py:25-97` | F.b (Body Systems missing HAI consumer) | UNCONNECTED to HAI plane | Body Systems |
| `ImpactEvent` | (referenced from `core/services/tool_dispatcher.py:1010-1035`) | F.b (Revenue Opportunity missing HAI consumer) | UNCONNECTED to HAI plane | Revenue Pipeline |

**Key observation:** The Cat F.d shadow services + F.d canonical writer plane + F.c PA-tool models operate on FIVE distinct model families (canonical Agent/UserAgentLearning/LearningInsight vs shadow Redis-only vs shadow AgentLearningEvent-tree vs pilot Experiment vs PA-tool internals) with NO shared source_kind provenance tagging — S1803 R8 codification candidate.

## 5. Major Services

Cat F.d service inventory + F.a external bridges + F.c PA service:

### 5.1 F.d shadow learning services (4 verified + 2 file-collision pairs = 6 entries)

| Service | File:Line | LOC | Liveness | Storage plane |
|---------|-----------|-----|----------|---------------|
| `AgentLearningService` | `core/services/agent_learning_service.py:122` | 795 | **LIVE-USED** (13 callers) | Redis ephemeral |
| `AgentLearningSystem` | `intelligence/agent_learning.py:22` | 272 | **LIVE-USED-FALLBACK** (1 try/except) | ORM read only |
| `AgentLearningEngine` | `ai_core/intelligence/agent_learning_engine.py:69` | 766 | **DEFINED-BUT-UNSCHEDULED** (Redis pubsub empty) | Redis + in-memory |
| `PersistentLearningEngine` | `ai_core/intelligence/persistent_learning_engine.py:42` | 456 | **DEFINED-BUT-UNUSED** (zero imports) | Django ORM (unreached) |
| `BoardroomLearningService` (variant A) | `core/services/boardroom_learning.py:27` | 449 | **LIVE** (3 callers) | — |
| `BoardroomLearningService` (variant B) | `core/services/boardroom_learning_service.py:32` | 420 | **LIVE** (4 callers) | — |
| `UnifiedLearningPipeline` (variant A) | `core/unified_learning_pipeline.py:9` | 55 | Likely legacy | — |
| `UnifiedLearningPipeline` (variant B) | `ai_core/intelligence/unified_learning_pipeline.py:54` | 609 | Likely active | — |

### 5.2 F.a external bridges

| Bridge | File:Line | LOC | Liveness | Structural note |
|--------|-----------|-----|----------|-----------------|
| `RedditLearningBridge` | `ai_core/intelligence/reddit_learning_bridge.py:96` | 998 | **DEFINED-BUT-UNSCHEDULED (on-demand LIVE)** | NOT `LearningBridge` ABC inheritor; standalone class |
| `BlueskyLearningBridge` | `ai_core/intelligence/bluesky_learning_bridge.py:108` | 938 | **DEFINED-BUT-UNSCHEDULED (near-dormant)** | NOT `LearningBridge` ABC inheritor; standalone class |

### 5.3 F.c PA service

| Service | File:Line | LOC | Liveness | Read/write scope |
|---------|-----------|-----|----------|------------------|
| `PALearningInsightsService` | `core/services/pa_learning_insights.py:24` | 447 | **LIVE (1 lazy-import caller at `pa_intelligence_enricher.py:551`)** | READ-ONLY (Experiment + ExperimentLearning) |

### 5.4 Cross-cat consolidation service map (per §7 six-plane runtime map)

Cat F consolidation surfaces SIX planes of learning-surface writer activity at HEAD (see §7.4 for runtime table):

1. **Plane 1 — Human-mediated canonical:** `FeedbackProcessor` → `AgentLearning` + `LearningInsight`. LIVE.
2. **Plane 2 — Autonomous canonical:** 9 `LearningBridge` subclasses + 4 non-bridge writers → `UserAgentLearning`. LIVE.
3. **Plane 3 — External intelligence aggregators:** `RedditLearningBridge` + `BlueskyLearningBridge` → dataclasses only. LIVE on-demand.
4. **Plane 4 — User-interaction ephemeral:** `AgentLearningService` → Redis only. LIVE.
5. **Plane 5 — Unscheduled async:** `AgentLearningEngine` → Redis pubsub (empty channels). DORMANT.
6. **Plane 6 — Unused ORM-tree:** `PersistentLearningEngine` → `AgentLearningEvent` family. UNUSED.

No shared schema. No `source_kind` provenance tag. No cross-plane query surface. This is the arc-wide FRAGMENTED-LEARNING-SURFACE consolidation finding.

## 6. Major APIs and Interfaces

Cat F has no direct APIs; the surface is consolidation-scope. However, the following external-facing entry points touched by Cat F sub-slots are re-cataloged here for xx99 §5 posture-decision brief consumption:

- **REST (from prior Cat audits):** `AttentionVerifyView.post` @ `core/views_human_interface.py:245` — user manual verification (S1805 §3). LIVE.
- **PA tool schemas (F.c intersection):** `learning_patterns_tool` @ `pa_tool_schemas.py:379`, `feedback_tool` @ `:404`, `learning_tool` @ `:1999`, `remember_tool` @ `:2126`. None reach into HAI/Feedback/Preference/AgentLearning writer plane.
- **Celery tasks / beat schedule:** ZERO beat entries for `RedditLearningBridge` or `BlueskyLearningBridge` continuous-learning tasks. ZERO beat entries for `AgentLearningEngine.start_agent_learning()` continuous processing. ZERO beat entries for `PersistentLearningEngine.batch_process_learning_signals()`. Cat F.d services + F.a external bridges are unscheduled at HEAD.
- **Management commands:** No dedicated management commands for F.a / F.c / F.d surfaces at HEAD.

## 7. Runtime Flows

### 7.1 F.a external-bridge runtime flow (on-demand pattern; DEAD scheduled path)

**LIVE path (Reddit only):**

1. Consumer (advisor / agent / consciousness) requests intelligence via singleton getter.
2. `get_reddit_learning_bridge()` returns cached instance.
3. Consumer calls `get_insights_for_advisor(advisor_name, keywords)` or `get_trending_topics()` or `get_community_consensus(topic)`.
4. Bridge queries `RedditHandler` @ `ai_core/spiders/reddit_handler.py` for external data.
5. Bridge returns in-memory dataclass (`RedditInsight` / `SubredditTrend` / `CommunityConsensus`).
6. Consumer processes dataclass in-context; no ORM write; no post_save signal; no learning-plane update.

**DEAD path (both bridges):**

- `start_continuous_learning()` @ `reddit_learning_bridge.py:197` + `bluesky_learning_bridge.py:151` defines 6-task parallel async monitoring loop but ZERO caller exists in production code. Infrastructure exists but is unreachable.

**Bluesky runtime:** Only registration site at `ai_core/consciousness/unified_mind.py:136`; no method invocation in production code observed.

### 7.2 F.b cross-domain gap illustrative flow (Failure Cluster → HAI, expected vs actual)

**Expected (post-gap-closure):**

1. `CeleryTaskEvent` accumulates FAILURE rows.
2. Failure-cluster aggregator detects N failures within window.
3. Aggregator calls `HumanAttentionBridge.create_failure_cluster_attention(cluster_signature, count, severity)`.
4. HAI row created with `item_type='failure_cluster'`.
5. User reviews; decides approve/reject/watch.
6. FeedbackProcessor consumes decision; learning-loop propagates.

**Actual at HEAD:**

1. `CeleryTaskEvent` accumulates FAILURE rows. ✓
2. NO aggregator exists — `core/services/body_coordinator.py:130-181` handlers all internal side-effect-free. ✗
3. No `create_failure_cluster_attention()` method — `human_attention_bridge.py:44-637` inventory has 10+ methods; none for failure clusters. ✗
4-6. Rest of flow unreachable.

**Same pattern applies to Signal Pattern, Body Systems, Revenue Opportunity, KillSwitch gaps** — producer domain has data; HAI writer method missing; downstream flow unreachable.

### 7.3 F.c PA-tool learning invocation runtime flow (LIVE via lazy import)

**LIVE path:**

1. PA (Rigby) processes user message via `unified_pa_entrypoint.py`.
2. PAIntelligenceEnricher's context-building path @ `core/services/pa_intelligence_enricher.py:544-559` reaches `PALearningInsightsService` block.
3. Lazy import: `from core.services.pa_learning_insights import PALearningInsightsService` @ `:550`.
4. Instantiate: `service = PALearningInsightsService()` @ `:551`.
5. Call `get_learning_insights(message, user_context)`.
6. Service routes to `_get_active_pilots()` @ `pa_learning_insights.py:197` — queries `Experiment.objects.filter(status__in=[…])`.
7. Or routes to `_find_similar_experiments(topic, themes)` @ `:249-251` — queries `Experiment.objects.filter(query).select_related('learning')` for join to ExperimentLearning.
8. Formats string summary; returns to enricher.
9. Enricher injects into PA context; PA responds with pilot/experiment intelligence.

**READ-ONLY throughout.** No HAI/Feedback/Preference/AgentLearning writes.

### 7.4 Cat F consolidation six-plane runtime map (Rigby SIGN cycle 1 codification candidate — three-plane table from S1805 §7.4 extended)

At HEAD, learning-surface writers span SIX planes with distinct liveness + storage + coupling characteristics:

| Plane | Writer(s) | Model target | Liveness | Coupling to HAI | Retention posture |
|-------|-----------|--------------|----------|-----------------|-------------------|
| **1. Human-mediated canonical** | `FeedbackProcessor` @ `models_feedback_processing.py:33-330` | `AgentLearning` + `LearningInsight` (core Group 1300) | LIVE | STRONG (HumanFeedbackRecord.post_save receiver); BUT preference-update chain BROKEN per S1804 F4 | SAVED-FOREVER (no cleanup task) |
| **2. Autonomous canonical** | 9 `LearningBridge` subclasses + 4 non-bridge writers @ 22+ sites (S1803 §3.2) | `UserAgentLearning` (core Group 1300) | LIVE | ZERO (S1803 §1 finding — 100% autonomous) | SAVED-FOREVER |
| **3. External intelligence aggregators** | `RedditLearningBridge` + `BlueskyLearningBridge` @ `ai_core/intelligence/*.py` | dataclasses only; NO DB writer | LIVE-ON-DEMAND (Reddit); NEAR-DORMANT (Bluesky) | ZERO | N/A (in-memory only) |
| **4. User-interaction ephemeral** | `AgentLearningService` @ `core/services/agent_learning_service.py:122` | Redis only; `AgentMemory` in-memory | LIVE | ZERO | Redis TTL (implicit) |
| **5. Unscheduled async** | `AgentLearningEngine` @ `ai_core/intelligence/agent_learning_engine.py:69` | Redis pubsub + in-memory knowledge_base | DORMANT (empty channels) | ZERO | N/A (unfired) |
| **6. Unused ORM-tree** | `PersistentLearningEngine` @ `ai_core/intelligence/persistent_learning_engine.py:42` | `AgentLearningEvent` + `LearningDocument` + `LearningEmbedding` + `AgentKnowledgeBase` + `AgentLearningSession` (ai_intelligence app) | UNUSED (zero imports) | ZERO | N/A (unwritten) |

**Consolidation verdict:** Planes 1-2 are canonical; Planes 3 (Reddit LIVE-ON-DEMAND) is intentional structural separation; Planes 4-6 are three progressively worse degrees of shadow-service accretion. **Six-plane fragmentation** at HEAD blocks single-query "what did we learn?" surface. R5 source_kind enum + R1 F.d dedup ADR target this.

### 7.5 Cat F durable-at-three loop-coupling break flow (from F7)

Combining S1801 D5 (inherited) + S1804 F4 + S1805 F4:

1. User sees HAI row; makes decision (approve/reject/defer/watch) via REST.
2. `HumanInterfaceService.record_decision()` @ `core/services/human_interface_service.py:295-353` writes `HumanFeedbackRecord`.
3. `HumanFeedbackRecord.post_save` receiver fires FeedbackProcessor @ `models_feedback_processing.py:372-392`.
4. FeedbackProcessor writes `AgentLearning` + `LearningInsight`.

**Break-point A (S1804 F4):** FeedbackProcessor does NOT invoke `HumanPreference.update_learned_stats()`. Preference-plane never updates from feedback. Personalization ranking stale.

**Break-point B (S1804 F4):** Auto-approve path @ `human_attention_lifecycle.py:325` bypasses `record_decision()` entirely — creates HumanFeedbackRecord directly via `.create()`. Auto-approved feedback records propagate to FeedbackProcessor but never touch preference-plane.

**Break-point C (S1805 F4):** For WATCH decisions leading to verification: user manual verify via REST @ `views_human_interface.py:245` calls `record_verification()` @ `models_human_interface.py:216-227` — plain `.save()`, no post_save receiver. `SportsBettingLearningBridge` never invoked. Verification outcome never propagates to learning-plane.

**Break-point D (S1805 F1 operational):** Even the automated verifier path (`verify_betting_outcomes` @ `core/tasks.py:6121`) has NO beat schedule entry per grep of `core/celery.py`. Zero fires in 30d per S1503 telemetry.

**Compound: four break-points in ONE loop. "Only round-trip w/ learning" is a phantom round-trip.**

## 8. Data Ownership and Lifecycle

Cat F is consolidation-scope; data ownership consolidates prior-child audit verdicts:

- **HumanAttentionItem** — Cat A (S1801 §8) — SAVED-FOREVER + bounded hard-delete for spider_action 6h + arbitrage 12h. Producer-side personalization DOES NOT respect user preferences (S1801 F2; F.b + F8 inheritance).
- **HumanFeedbackRecord** — Cat B (S1802 §8) — SAVED-FOREVER; `fed_to_ml` lifecycle partial. F.d F.e shadow: none of Cat F.d services touch this model.
- **HumanPreference** — Cat D (S1804 §8) — governance plane WORKING; learning plane BROKEN (F5 two-part bug); notification plane DEAD.
- **AgentLearning + UserAgentLearning + LearningInsight** — Group 1300 territory; Cat B (S1802) writer for AgentLearning + LearningInsight; Cat C (S1803) writer for UserAgentLearning. None of Cat F.d shadow services write to these.
- **Cat F.d shadow models** — `AgentLearningEvent` + `LearningDocument` + `LearningEmbedding` + `AgentKnowledgeBase` + `AgentLearningSession` @ `ai_core/intelligence/models.py` — UNWRITTEN at HEAD. Lifecycle policy undefined (no cleanup task; no retention framework). Post-arc T-slot inheritance.

**Consolidation ownership pattern:** the SIX-plane fragmentation (§7.4) manifests here as SIX distinct retention/lifecycle regimes with NO unified retention framework. R5 codification candidate.

## 9. Integrations With Other Domains

Cat F consolidates cross-domain HAI integrations. Complete matrix inherits from S1801 §9 + S1274 §3.3 + §3.8:

| Producer Domain | HAI Consumer State | Classification | Cat F sub-slot | Severity |
|-----------------|-------------------|----------------|----------------|----------|
| Signal Engine (9) | HAI (16) | **MISSING** @ §3.8:526 | F.b | MEDIUM |
| Body Systems (30) | HAI (16) | **MISSING** @ §2.2:295 | F.b | MEDIUM |
| Revenue Pipeline (32) | HAI (16) | **MISSING** @ §2.4:317 | F.b | MEDIUM |
| Observability (25) — Failure Cluster | HAI (16) | **MISSING** @ §3.3:438 | F.b | CRITICAL/HIGH |
| Governance (23) / KillSwitch | HAI (16) | **WEAK** @ §2.6:336 | F.b | MEDIUM |
| Employee OS / MissionRunner | HAI (16) | **MISSING** (uncataloged) | F.b | MEDIUM |
| Discord Bot | HAI (16) | **MISSING** (uncataloged) | F.b | LOW-MEDIUM |
| Sports/DBAO — non-arbitrage | HAI (16) | **MISSING** (uncataloged) | F.b | MEDIUM |
| PA (Rigby) | HAI/Feedback/Preference | **NONE** (boundary CLEAN per S1605) | F.c | (correct boundary) |
| External social (Reddit/Bluesky) | HAI/Feedback/Preference | **NONE** (deliberate structural separation) | F.a | (correct boundary) |
| Cat F.d shadow services (4) | HAI/Feedback/Preference | **NONE** (canonical writers are FeedbackProcessor + LearningBridges only) | F.d | (correct isolation) |

Positive contrast — 10+ LIVE producer methods @ `human_attention_bridge.py` per §3.2.

## 10. Event Flows

Cat F consolidation reveals NO event surface at HEAD:

- **F.a external bridges emit NO events / NO signals / NO logs to HAI plane.** Return dataclasses only.
- **F.d shadow services (4) emit NO events to canonical AgentLearning/UserAgentLearning writer plane.** AgentLearningEngine sets up Redis pubsub but zero publishers exist. PersistentLearningEngine is unreached.
- **F.c PALearningInsightsService emits NO events.** READ-ONLY.
- **F.b MISSING integrations means no cross-domain event → HAI wire exists** (Failure Cluster, Signal Pattern, Body Systems, Revenue Opportunity, KillSwitch all fail-quiet at HAI boundary).
- **F.e verification-as-HAI-field** — S1805 F4 durable-at-two: `record_verification()` emits NO signal / NO event / NO log.

**Consolidation pattern:** The Group 1800 arc has a durable-at-six event-emission gap — no writer in any of Cat A/B/C/D/E/F sub-slots emits structured events or logs at learning-plane transitions. This is the load-bearing driver for R5 source_kind enum + Group 1900 Event Architecture arc scoping. **xx99 §8 T-slot: Group 1900 Event Architecture inherits Group 1800 event-gap catalog as arc-close handoff.**

## 11. Existing Documentation

Cat F sub-slot documentation coverage (per Explore Agent 6 F.6.1 matrix):

| Sub-slot | Coverage tier | Evidence |
|----------|---------------|----------|
| F.a External bridges | **LIGHT** | Parent §2.4 lists both bridges; S1803 §3.2 references. Zero topic doc in `docs/topics/`. Zero PLATFORM_INVENTORY narrative. |
| F.b Cross-domain HAI-consumer gaps | **MODERATE** | `docs/research/platform/cross_domain_integration_audit.md` §3.3 + §3.8 explicit MISSING catalog; parent §2.3 reiterates. |
| F.c PA-tool learning integration | **MODERATE** | `docs/topics/personal-assistant.md` covers PA overall; PALearningInsightsService NOT enumerated in enrichment table (lines 65-76). Session 605 docstring is the only native comment. |
| F.d Duplicate learning-service inventory | **LIGHT** | S1273 §5.13 mentions "dedup audit needed"; S1803 F4 surfaced collision. Zero canonical consolidation doc. |
| F.e Terminology boundary | **NONE** | No `docs/glossary.md`. Parent §2.6 F5 primitive table not a glossary. Onboarding risk. |

**Arc-wide documentation debt: LIGHT-to-MODERATE average with F.e at NONE.** xx99 §7 anchor-update inheritance includes glossary candidate.

## 12. Research Coverage

Cat F sub-slots research coverage classification per playbook §12:

- **F.a** — LIGHT (S1803 §3.C brief mention; S1806 first dedicated audit)
- **F.b** — MODERATE (S1274 v3 dedicated cross-domain audit + S1806 arc-close consolidation)
- **F.c** — MODERATE (Group 1600 S1605 tactical-split precedent + S1806 dedicated audit)
- **F.d** — LIGHT (S1273 §5.13 mention + S1803 §14 F4 surfaced + S1806 dedicated audit)
- **F.e** — LIGHT (parent §2.6 primitive table + S1806 dedicated matrix)

Cat F as a whole promotes Group 1800 arc-close research coverage from MODERATE (Cat A-E children) to **MODERATE-with-arc-close-consolidation** upon S1806 close + S1899 canonical summary landing.

## 13. Architecture Maturity

Cat F maturity per sub-slot (playbook §12):

- **F.a** — **EXPERIMENTAL** (code compiles; on-demand fetch pattern LIVE for Reddit; `start_continuous_learning()` dead code; Bluesky near-dormant)
- **F.b** — **PARTIAL** (Rigby SIGN cycle 1 downgrade from WORKING → PARTIAL: catalog works but integration does NOT — enforcement/monitoring absent; gaps are catalogued but closure DEFERRED to xx99 §8 T-slot ADR)
- **F.c** — **PARTIAL** (service defined; single caller LIVE via lazy import; topic-doc silent; zero test coverage; deployment status intact but discoverability low)
- **F.d** — **WORKING-runs-without-immediate-errors with HIGH shadow debt** (Rigby SIGN cycle 1 explicit-qualification: "WORKING" scoped to runtime-doesn't-crash, NOT coherent-architecture; 3-of-4 shadow services stale/dormant; 2 duplicate-file class-name collisions)
- **F.e** — **EXPERIMENTAL** (research foundational; boundary codification INCOMPLETE; xx99 posture-decision must frame post-arc terminology ADR)

**Arc-wide Cat F consolidation maturity: PARTIAL-to-WORKING with EXPERIMENTAL consolidation research.**

## 14. Known Drift

Cat F consolidation drift matrix:

| Sub-slot | Doc claim | Runtime reality | Drift severity |
|----------|-----------|-----------------|----------------|
| F.a | Parent §2.4 implies both external bridges "in `ai_core/intelligence/`" as part of learning-bridge inventory | Both STANDALONE classes; NEITHER inherits `LearningBridge` ABC; on-demand pattern not autonomous | **MED** structural |
| F.b | S1274 §2.5 says "5+ MISSING HAI-consumer gaps" | Verified 5 explicit + 3 uncataloged candidates surfaced by S1806 | **LOW** (count verified) |
| F.c | Topic doc `personal-assistant.md` enrichment table lists 8 services; PALearningInsightsService NOT listed | Service EXISTS + is used (single caller via lazy import); topic doc silent | **MED** discoverability |
| F.d | Parent §2.4 scoped 4 shadow services + S1803 F4 surfaced "16+ classes across 3 apps"; parent count under-represents | S1806 verifies 4 primary shadows + 2 duplicate-file class-name collisions (Boardroom + UnifiedLearningPipeline) | **HIGH** consolidation |
| F.e | Parent §5 F.e says "6 overlapping-but-distinct terms" | S1806 pre-Explore CORRECTED to 5 semantic terms + verification-as-HAI-field | **HIGH** terminology |
| Migration drift | Parent §2.4 claimed `AgentLearningSession` deletion @ S1244 migration 0002 | S1803 F3 verify: only `core` variant deleted per `368_session_1244_delete_agentlearningsession.py`; `ai_intelligence.AgentLearningSession` model ALIVE + written by `persistent_learning_engine.py:65` (BUT engine unused so writes never happen) | **LOW** (nested drift; engine unused mitigates) |

**Cat F consolidation drift summary:** 2 HIGH (duplicate file collision + terminology 5-not-6 correction), 2 MED (structural mislabeling + discoverability silence), 2 LOW (count + migration nested drift). Arc-wide: HIGH consolidation debt.

## 15. Known Technical Debt

Cat F debt matrix (extended from Explore Agent 6 F.6.3):

| # | Debt item | Evidence file:line | Severity |
|---|-----------|--------------------|----------|
| D1 | `BoardroomLearningService` file-name collision (2 files, same class) | `core/services/boardroom_learning.py:27` + `boardroom_learning_service.py:32` | **HIGH** |
| D2 | `UnifiedLearningPipeline` file-name collision (2 files, same class) | `core/unified_learning_pipeline.py:9` + `ai_core/intelligence/unified_learning_pipeline.py:54` | **HIGH** |
| D3 | `PersistentLearningEngine` DEFINED-BUT-UNUSED (zero imports) | `ai_core/intelligence/persistent_learning_engine.py:42-456` | **MED-HIGH** (dead code candidate for S1244 cleanup pattern) |
| D4 | `AgentLearningEngine` async loop unwired (Redis pubsub empty channels) | `ai_core/intelligence/agent_learning_engine.py:202-221, :282` | **MED** |
| D5 | `AgentLearningSystem` fallback-only status; unclear ownership vs Sports arc | `intelligence/agent_learning.py:22`; sole caller `intelligence/agent_executor.py:1` | **MED** |
| D6 | `RedditLearningBridge` + `BlueskyLearningBridge` `start_continuous_learning()` dead code | `reddit_learning_bridge.py:197`; `bluesky_learning_bridge.py:151` | **MED** |
| D7 | `PALearningInsightsService` zero test coverage | `core/services/pa_learning_insights.py:24` (447 LOC untested) | **MED** |
| D8 | `PALearningInsightsService` NOT in `personal-assistant.md` enrichment table | `docs/topics/personal-assistant.md:65-76` | **MED** discoverability |
| D9 | `LearningOrchestrator._initialize_bridges()` MISSING `AutoConsultationLearningLoop` registration | S1803 F8 inheritor | **MED** |
| D10 | 5 explicit MISSING cross-domain HAI-consumer gaps (F.b) | Cross-domain integration audit §3.3, §3.8, §2.2, §2.4, §2.6 | **HIGH-MED** (5-item aggregate) |
| D11 | Producer-side personalization enforcement absent (durable-at-three: S1801 F2 + S1804 F4 + F.b sample) | `human_attention_bridge.py:80-150` — 43 producer paths, zero HumanPreference checks | **MED** |
| D12 | Compound learning-loop false-confidence — 4 break-points (F7) | S1801 D5 + S1804 F4 + S1805 F4 + F.d shadow-plane isolation | **CRITICAL** architectural |
| D13 | Zero test coverage on Cat F surfaces (durable-at-six under Group 1800) | `core/tests/` grep zero for reddit/bluesky/PA-learning/shadow-services | **LOW-MED** durable |
| D14 | No `docs/glossary.md`; terminology boundary undefined (F.e) | (absence) | **HIGH** onboarding |
| D15 | Six-plane learning-surface fragmentation (no source_kind enum; no cross-plane query) | §7.4 table | **HIGH** architectural |
| D16 | `AgentLearningSession` migration drift (`ai_intelligence` variant survives; `core` variant deleted) | S1803 F3 inherited | **LOW** (unused mitigates) |

**Cat F debt severity roll-up:** 1 CRITICAL (D12 loop-coupling), 4 HIGH (D1 + D2 collisions; D14 + D15 architectural), 6 MED-to-MED-HIGH, 2 LOW-MED durable, 1 LOW. **Total: 16 items.** Post-arc T-slot inheritance for xx99 §8.

## 16. Boundary Violations

Cat F consolidation-scope boundary discipline check:

- **F.a boundary discipline (`ai_core/intelligence/` external-signal zone vs `core/learning_bridges/` internal-event learning-write plane):** DELIBERATE structural separation; NOT a violation. Naming shared "LearningBridge" suffix creates false structural equivalence at doc level — recommend rename (R3).
- **F.c boundary discipline (S1605 tactical-split: PA tool surface vs canonical object model):** CLEAN — no PA tool reaches into HAI/Feedback/Preference/AgentLearning writer plane. Boundary intact per S1605.
- **F.d boundary discipline (shadow services vs canonical FeedbackProcessor + LearningBridges writer plane):** CLEAN at writer-plane level (no shadow writes canonical models); VIOLATED at class-name uniqueness level (D1 + D2 duplicate file collisions).
- **F.e boundary discipline (permeable-by-design terminology):** PERMEABLE-BROKEN in three places (learning ↔ personalization; preference ↔ personalization; verification ↔ learning). Per S1706 F8 precedent, permeability is intended; the breaks are implementation gaps (F7 loop-coupling).
- **F.b boundary discipline (cross-domain producer → HAI consumer):** NOT VIOLATED — the boundary is CORRECTLY MISSING (5 explicit MISSING integrations catalogued; each has a named reason per S1274 §3.3-§3.8). Gap catalog is mature research debt, not boundary violation.

**Overall boundary posture:** No arc-wide boundary violations at HEAD. Two file-name-collision cases (D1 + D2) violate the class-name-uniqueness constraint but not the domain-boundary constraint.

## 17. Duplicate or Overlapping Systems

Cat F.d is the arc-close duplicate-systems audit for Group 1800. Comprehensive inventory:

**Canonical writer plane (no overlap):**
- `FeedbackProcessor` — sole human-mediated writer to `AgentLearning` + `LearningInsight`
- 9 `LearningBridge` subclasses — sole autonomous writer plane to `UserAgentLearning`

**Shadow overlap plane (F.d finding):**
- 4 shadow services with overlapping "record interaction / calibrate behavior / track metrics" capabilities (per Explore Agent 4 §F.d.5 capability matrix):
  - AgentLearningService: record_interaction + preference-application + adaptive-context (Redis-backed)
  - AgentLearningSystem: confidence_adjustment + should_predict (ORM-backed read-only)
  - AgentLearningEngine: process_learning_signal + knowledge_base (Redis + in-memory, unscheduled)
  - PersistentLearningEngine: process_learning_signal + batch + persistence (ORM writer, unused)
- Overlap is REAL but functional divergence exists (different storage planes, different signal types).
- NO shared base class or contract. NO shared source_kind enum.

**Duplicate-file class-name collision (D1 + D2):**
- BoardroomLearningService (2 files, same class name)
- UnifiedLearningPipeline (2 files, same class name)

**Canonical-verdict candidate (Explore Agent 4 §F.d.6):**
1. KEEP: `AgentLearningService` (LIVE-USED, 13 callers)
2. CLARIFY OWNERSHIP with S1599 Sports arc: `AgentLearningSystem` (fallback-only; may belong to Sports)
3. DEPRECATE-CANDIDATE: `AgentLearningEngine` (infrastructure exists but unfed data)
4. HARD-DELETE-CANDIDATE: `PersistentLearningEngine` (zero callers; S1244 dormant cleanup pattern)
5. CONSOLIDATE-COLLISIONS: `BoardroomLearningService` + `UnifiedLearningPipeline` pairs

**R1 F.d consolidation ADR post-arc T-slot inheritance.**

## 18. Ownership Gaps

Cat F ownership matrix (extended from Explore Agent 6 F.6.4):

| Sub-slot | Current owner | Clarity | Notes |
|----------|---------------|---------|-------|
| F.a external bridges | UNCLEAR | UNCLEAR | No CLAUDE.md mention; no recent handoff activity; candidate: intelligence subsystem |
| F.b HAI-consumer gaps | GOVERNANCE-SCOPE (deferred to gap-closure) | CLEAR | Ownership: whoever closes the gap per post-arc ADR |
| F.c PALearningInsightsService | PA subsystem (Rigby) | MODERATE | Service written Session 605; docstring mentions PA context; invocation site verified but topic-doc silent |
| F.d duplicate learning services | FRAGMENTED | UNCLEAR | 3-app split (`core/services/`, `ai_core/intelligence/`, `intelligence/`); no canonical owner role documented |
| F.e terminology boundary | RESEARCH-SCOPE (S1800-S1806) | CLEAR | Ownership DEFERRED to xx99 posture-decision + post-arc ADR |

**Ownership clarity roll-up: 2 CLEAR + 1 MODERATE + 2 UNCLEAR-to-FRAGMENTED.** Post-arc T-slot: R1 F.d dedup ADR must assign single owner. R3 F.a naming ADR clarifies structural boundary owner.

## 19. Recommended Future Research

Cat F R-slots for post-arc Chris-gate ordering (proposed: R0 → R1 → R2 → balance):

- **R0 (SYSTEMIC POST-ARC — D80 posture-decision framing update)** — F7 CRITICAL constraint (compound learning-loop false-confidence pattern; 4 break-points) elevates D80 posture-decision framing at xx99 §5. Recommend xx99 §5 posture-decision brief EXPLICITLY frames D80 selection as "with-assumptions-pending-fix" (Option A/B/C/D each with named pre-conditions to satisfy pre-selection) OR defers final selection post-T-slot. R0 gates all subsequent R-slot ordering.
- **R-slot next-step gating (Rigby SIGN cycle 1 fold — Chris-gate ordering):** **cannot make D80 posture decision until (a) F.d duplicate collisions resolved OR isolated (R1) AND (b) F7 loop coupling re-established / observed (R7).** R0 depends on R1 + R7; R2 onward can proceed in parallel per domain-specific ordering.
- **R1 (HIGH — F.d duplicate learning-service consolidation ADR)** — canonical-verdict per Explore Agent 4 §F.d.6: KEEP AgentLearningService; CLARIFY OWNERSHIP AgentLearningSystem; DEPRECATE-CANDIDATE AgentLearningEngine; HARD-DELETE-CANDIDATE PersistentLearningEngine; CONSOLIDATE-COLLISIONS BoardroomLearningService + UnifiedLearningPipeline pairs.
- **R2 (HIGH — F.b cross-domain HAI-consumer integration closure ADR bundle)** — 5 explicit MISSING (Failure Cluster / Signal Pattern / Body Systems / Revenue Opportunity / Governance KillSwitch) + 3 uncataloged candidates (Employee OS mission-failure / Discord command-failure / Sports non-arbitrage outcomes). Per-domain consumer contracts + threshold-based auto-escalation.
- **R3 (MED — F.a external-bridge naming clarity ADR)** — rename `RedditLearningBridge` → `RedditIntelligenceBridge` + `BlueskyLearningBridge` → `BlueskyIntelligenceBridge` to distinguish from `core/learning_bridges/` writer plane. Delete `start_continuous_learning()` dead code from both bridges.
- **R4 (LOW-MED — F.c PALearningInsightsService topic-doc update)** — add to `docs/topics/personal-assistant.md` enrichment table; document lazy-import invocation vector at `pa_intelligence_enricher.py:544-559`.
- **R5 (HIGH — F.e source_kind enum schema-change ADR)** — S1803 R8 candidate; provenance tagging on AgentLearning + UserAgentLearning rows to distinguish (a) human-mediated (FeedbackProcessor), (b) autonomous-domain (9 core bridges), (c) verification-outcome (SportsBettingLearningBridge via record_verification), (d) external-signal (Reddit/Bluesky if wired), (e) shadow-service (if any of F.d 3 shadows write in future). Enables cross-plane query surface.
- **R6 (MED — F.e terminology glossary ADR)** — 5-semantic-term matrix + verification-as-HAI-field codification; produce `docs/glossary.md` for onboarding. xx99 §5 posture-decision brief consumer.
- **R7 (CRITICAL — loop-coupling repair ADR bundle)** — S1804 F4 + S1805 F4 + S1801 D5 durable-at-three consolidation. Post_save receivers on `HumanFeedbackRecord` (fires update_learned_stats) + `HumanAttentionItem` STATUS_VERIFIED transitions (fires learning bridge). Loop test coverage.
- **R8 (MED — Producer-side personalization enforcement ADR)** — S1801 F2 + S1804 F4 + F.b sample durable-at-three. `HumanAttentionBridge.create_item()` should filter against `HumanPreference.blocked_sources` + `min_urgency_to_notify` + `trusted_agents` + `require_review_above_confidence` before emit.
- **R9 (LOW-MED — F.c PAToolInsight scope investigation + feedback_tool .save() call scope investigation)** — verify `PAToolInsight` model FK graph (does it link back to HAI/AgentLearning?); verify `feedback_tool` `.save()` call at `core/services/td_handlers_content.py:3538` scope.
- **R10 (LOW — F9 durable-at-six test-gap arc-close ADR bundle)** — unified test-suite scope + regression harness for Cat F surfaces (F.a bridges + F.c PA service + F.d shadow services + F.b integrations). Extends S1801 R7 + S1802 R7 + S1803 R7 + S1804 R7 + S1805 R7 durable pattern.
- **R11 (LOW — F10 durable-at-six coverage-query dashboard)** — personalization-respect + cross-domain-integration + service-liveness observability surface. xx99 §7 anchor-update inheritance.
- **R12 (MED — F.d migration drift cleanup ADR)** — `AgentLearningSession` `ai_intelligence` variant deletion (paired with R1 PersistentLearningEngine hard-delete; the engine writes to this model but is unused, so both die together cleanly).

## 20. Appendix

### 20.1 Files inspected

Direct grep + Read verification at HEAD `eb6d5fcd`:

- `ai_core/intelligence/reddit_learning_bridge.py` (998 LOC) — F.a
- `ai_core/intelligence/bluesky_learning_bridge.py` (938 LOC) — F.a
- `core/services/pa_learning_insights.py` (447 LOC) — F.c
- `core/services/pa_intelligence_enricher.py:544-559` (lazy import site) — F.c
- `core/services/agent_learning_service.py` (795 LOC) — F.d
- `intelligence/agent_learning.py` (272 LOC) — F.d
- `ai_core/intelligence/agent_learning_engine.py` (766 LOC) — F.d
- `ai_core/intelligence/persistent_learning_engine.py` (456 LOC) — F.d
- `core/services/boardroom_learning.py:27` + `boardroom_learning_service.py:32` — F.d duplicate collision
- `core/unified_learning_pipeline.py:9` + `ai_core/intelligence/unified_learning_pipeline.py:54` — F.d duplicate collision
- `core/services/human_attention_bridge.py:44-637` — 10+ WORKING HAI producer methods (F.b positive contrast)
- `core/services/body_coordinator.py:130-181` — F.b 23 handlers, zero HAI emit
- `core/services/signal_curator_service.py:1-574` — F.b no HAI threshold gate
- `core/models_heart.py:25-97` — F.b HeartBeat unconnected
- `docs/research/platform/cross_domain_integration_audit.md` §2, §3.3, §3.8, §2.4, §2.6, §2.7 — F.b baseline
- `docs/research/domains/human_attention/1800_human_attention_domain_scoping.md` §3.F, §5, §2.6 — parent
- Prior children S1801 + S1802 + S1803 + S1804 + S1805 audit docs (executive summaries + §14 debt + §19 R-slots) — arc consolidation
- `docs/topics/personal-assistant.md` §4 enrichment table (lines 65-76) — F.c drift check
- `core/services/pa_tool_schemas.py:379-434, 1999-2040, 2126-2177` — F.c PA-tool boundary
- `core/services/td_handlers_content.py:3314-3538` + `td_handlers_core.py:1896-2192` — F.c PA-tool handler layer

### 20.2 Docs inspected

- Playbook §11.2 20-section template (eleventh application overall)
- Playbook §13 6-parallel-Explore agent template
- Playbook §14 verifier-loop REQUIRED CODIFICATION-READY (S1799 §10.2 MC-1)
- Playbook §15 SIGN cycle 1 single-batch 4-question routing
- Playbook §16 pin discipline (arc-pin routing precedent durable-by-fifth-application; S1806 confirms durable-by-sixth)
- Playbook §21 short-command intent interpretation

### 20.3 Grep patterns used

**Pre-Explore verifier grep (Cat F sub-slot existence):**

```
rg -l "RedditLearningBridge|BlueskyLearningBridge" --type py
rg -l "^class AgentLearningService|^class AgentLearningSystem|^class AgentLearningEngine|^class PersistentLearningEngine" --type py
rg -l "PALearningInsightsService" --type py
```

**Post-Explore verifier grep (Agent 3 vs Agent 6 conflict resolution + duplicate collision confirmation):**

```
rg -n "PALearningInsightsService" --type py
rg -n "class BoardroomLearningService" --type py
rg -n "class UnifiedLearningPipeline" --type py
```

**Post-SIGN Q1 miss-vector rule-out grep (pending SIGN cycle 1; will populate here at fold-time):**

```
[to be populated post-SIGN per playbook §14 verifier-loop REQUIRED discipline]
```

### 20.4 Unresolved unknowns

- **F.c PAToolInsight model FK graph** — does it link back to HAI/AgentLearning/UserAgentLearning? (R9 post-arc verification)
- **F.c `feedback_tool` `.save()` call scope at `td_handlers_content.py:3538`** — LIST/STATS action or actual WRITE? (R9 post-arc verification)
- **F.d `AgentLearningSystem` ownership** — belongs to Sports arc (S1599) or Learning arc (Group 1800)? (R1 sub-decision post-arc)
- **F.d `AgentLearningEngine` intended publisher** — which task was supposed to publish to `learning:signals:*` channels? (R1 sub-decision post-arc)

### 20.5 Conflicts between sources

**RESOLVED at post-Explore verifier:**

- **Agent 3 vs Agent 6 on PALearningInsightsService caller sites** — Agent 3 claimed "1 caller via lazy import at pa_intelligence_enricher.py:551"; Agent 6 claimed "grep returns ZERO call sites". **Resolution:** Direct grep at HEAD `eb6d5fcd` confirmed Agent 3 CORRECT — `core/services/pa_intelligence_enricher.py:544-559` shows `from core.services.pa_learning_insights import PALearningInsightsService` (lazy import inside try/except) + `service = PALearningInsightsService()` at `:551`. Agent 6's grep pattern missed lazy imports. **Post-Explore verifier LOOP APPLIED per playbook §14 CODIFICATION-READY.**

- **Parent §5 F.e "six terms" claim** — pre-Explore verifier corrected to 5 semantic terms + verification-as-HAI-field. Verification is a HAI schema field-level property (`verification_outcome`, `verified_at`, `verification_profit`, `verification_notes`, `event_completed_at`) invoked via `record_verification()`, NOT a standalone learning-signal primitive with its own model. F.e.1 terminology matrix reflects the correction.

- **Cross-domain integration audit MISSING count** — Agent 6 said "5 MISSING per doc"; Agent 2 said "7 MISSING (5 verified + 2 uncataloged)". **Resolution:** Both correct at different scope. 5 explicit MISSING in `cross_domain_integration_audit.md` at HEAD; 3 uncataloged candidates (Employee OS + Discord + Sports non-arbitrage) surfaced by S1806. F.b catalog: 5 explicit + 3 candidate = 8 total gap catalog.

### 20.6 Verifier-loop corrections (Rigby SIGN fold notes)

**Rigby SIGN cycle 1 SIGN-with-edits at High confidence 0.82 (2026-07-04) on arc pin `pa-ae5931ea706b4537`.** Turn 1 CLEAN. D48 30th arm turn 1 CLEAN. 25th consecutive-fully-clean-arms sub-pattern CONFIRMED per single-batch 4-question criterion (S1799 §10.2 MC-2 CODIFICATION-READY promotion path continues advancing).

**Rigby SIGN cycle 1 folds landed pre-commit:**

- **§1 biggest-risk RISK-SPLIT paragraph fold** — added explicit F7 strategic vs F3 immediate-correctness distinction to prevent reviewers arguing past each other; F7 = highest strategic risk (blocks D80); F3 = highest immediate risk (silent import binding).
- **§1 canonical-plane one-paragraph statement fold** — added "de facto canonical writers at HEAD" statement for xx99 §5 D80 consumers to reason "what exists" vs "what's aspirational" (`FeedbackProcessor` + 9 core `LearningBridges` canonical; 4 Cat F.d shadows are partial/legacy/experimental with no ADR endorsement).
- **F1 intentional-separation + dead-hook reframing fold** — labeled F.a boundary as INTENTIONAL SEPARATION (not accidental drift); `start_continuous_learning()` as abandoned/dead integration hook (not evidence boundary "should" be integrated).
- **F2 verified-set + candidate-set + search-scope softening fold** — softened "5 explicit + 3 uncataloged" to "verified set of 5 + candidate set of 3 under scanned patterns; additional gaps likely in other desks not scanned"; added explicit search-scope explanation (governance/autopilot kill-switches + messaging inbox/SLA + content-desk HAI producers as unscanned likely-additional-gap territory).
- **F3 silent-import-collision mechanics explicitness fold** — expanded "import-path-dependent silent binding risk" to explicit mechanics: Python module path determines binding; type checkers + linters + tests do NOT catch mismatch; refactoring an import rebinds to structurally different class with different semantics; no compile-time signal.
- **F4 no-canonical-writer-evidence + no-ADR-endorsement fold** — added "none of the four services write to canonical AgentLearning/UserAgentLearning models — supports shadow interpretation"; added "no ADR at HEAD endorses multiple canonical learning writers; therefore treat multiplicity as debt until proven intentional."
- **F7 phantom-learning-loop 2-line tie-back fold** — added "creates phantom learning loop" framing: blocks single query "what did we learn about Agent X across all signal types?" + creates phantom round-trip where code paths exist at all four layers but data never flows.
- **F.b maturity downgrade fold** — WORKING → PARTIAL: "catalog works but integration does NOT" per Rigby explicit downgrade (enforcement/monitoring absent).
- **F.d WORKING explicit-qualification fold** — "WORKING" scoped to "runs without immediate errors," NOT to "coherent architecture" — Rigby explicit-qualification-language.
- **§19 R-slot next-step-gating line fold** — added Chris-gate ordering constraint: "cannot make D80 posture decision until (a) F.d duplicate collisions resolved OR isolated (R1) AND (b) F7 loop coupling re-established / observed (R7)"; R0 depends on R1 + R7.

**Rigby SIGN cycle 1 miss-vector rule-out queue (Q1 fold candidates surfaced by Rigby):**

- **Non-*Learning*-suffix shadow services** — Rigby suggested checking for services named `*Loop`, `*Ingest`, `*Enricher`, `*Reconciler`, `*SignalEnricher`, `*ConsultationLoop`, `*ScoringEngine`, `*Attribution*`. See §20.9 extended miss-vector grep.
- **Non-obvious PA-tool learning routes** — Rigby suggested confirming no PA tool action triggers "recompute/refresh/enrich" that results in downstream writes. Verifier scope: PA `write_tool`, `remember_tool`, `learning_tool`. See §20.9.
- **Additional cross-domain HAI-consumer gaps in other desks** — Rigby suggested scanning governance/autopilot, messaging/inbox, content-desk. See §20.9.
- **"settlement/outcome" terminology alternative** — Rigby noted potential 6th term if some module uses "settlement" language instead of "verification." F.e.1 5-semantic-terms + verification-as-field posture appears CORRECT; "settlement" is subsumed under verification-as-HAI-field per S1805 verification_outcome enum.

Miss-vector rule-out grep sweep landed at §20.9 pre-commit.

**Rigby SIGN cycle 1 verdict summary:** Overall confidence High (0.82). Most accurate part: "shape of the problem" identification — multiple learning-ish planes with weak provenance + duplicate class-name collisions + durable false-confidence loop pattern; F3/F4/F7 well-chosen as arc-close leverage points; separation vs integration distinction at F.a framed thoughtfully. Weakest part: exhaustive completeness claims in a consolidation audit are the easiest to over-claim (addressed via F2 softening fold + §20.9 miss-vector grep extension).

### 20.7 F5 correlation-primitive HYPOTHESIS box discipline — ARC-CLOSE meta-methodology consolidation (per S1805 close directive: S1806 CONSUMES the F5 arc; does NOT test another F5 primitive)

**Per parent §2.6 + S1805 close directive:** S1806 Cat F is a CONSOLIDATION audit and does NOT test another F5 primitive. F5 primitive-testing arc COMPLETES at S1805 for Group 1800.

**Running tally across 5 applications S1801-S1805:**

| Application | Primitive | Result | Cross-System Verdict |
|-------------|-----------|--------|---------------------|
| S1801 Cat A | `HAI_item_id` | **PASS** — 8-10 cross-domain usage confirmed | CROSS-SYSTEM CONFIRMED |
| S1802 Cat B | `feedback_record_id` | FAIL | DOMAIN-INTERNAL |
| S1803 Cat C | `learning_event_id` | FAIL (4 ai_core hits + 6 serialization-string-tags) | DOMAIN-INTERNAL |
| S1804 Cat D | `user_pref_id` | FAIL (ZERO grep hits anywhere) | DOMAIN-INTERNAL |
| S1805 Cat E | `verification_id` | FAIL (1 hit at advisor_network_api.py:315, verified DIFFERENT object) | DOMAIN-INTERNAL |

**Running tally: 1 PASS / 4 FAIL. Utility rate: 5/5. Pass rate: 1/5.**

**Meta-methodology CONSOLIDATED FINDING for xx99 §10 CODIFICATION-CONFIRMED under utility-rate-vs-pass-rate framing:**

The F5 correlation-primitive HYPOTHESIS box discipline has been APPLIED 5 TIMES across Group 1800 child audits on 5 distinct model families. All 5 applications produced defensible verdicts — 1 genuine cross-system primitive (HAI_item_id, 8-10 domain usage) + 4 domain-internal primitives (confirmed via exhaustive grep + ORM spot-checks + serialization-string-tag distinction fold from S1803 SIGN cycle 1).

**Utility framing:** Each application correctly distinguished cross-system from domain-internal scopes. Discipline utility: 100%.

**Pass-rate framing:** 1/5 primitives passed cross-system test. Discipline pass rate: 20%.

**Meta-methodology verdict:** The 4 failures are CORRECT DIAGNOSES that the modeled primitives are domain-internal (NOT design failures of the discipline; the discipline correctly diagnosed all five). The pass-rate framing UNDERSELLS the discipline's value; the utility-rate framing captures it.

**Two distinct failure modes distinguished (S1806 consolidation)**:

- **Mode 1 (Cross-system absence)** — `feedback_record_id` / `learning_event_id` / `user_pref_id`: primitive is defined at schema level (as PK) but never referenced outside its originating domain. Suggests either (a) primitive was designed for cross-domain use but never consumed (technical debt), or (b) primitive was correctly scoped as domain-internal and should not be cross-referenced (working as designed). S1802 F5 + S1803 F5 + S1804 F5 = Mode 1.

- **Mode 2 (Primitive absence)** — `verification_id`: no explicit ID primitive defined at schema level (HAI.id covers verification status transitions, but no dedicated verification_id field exists). Suggests either (a) field is not semantically distinct from HAI.id (working as designed), or (b) opportunity for explicit tagging was missed (technical debt). S1805 F5 = Mode 2.

**Single PASS is load-bearing:** HAI_item_id is the only cross-system primitive found. It is used in 8-10 domains. Its absence would break multiple pipelines (mission_control_executor, orchestration, ops_autopilot, etc.). This single primitive carrying all the load suggests: either (a) the architecture is under-primitived (should have 3-4 more cross-system identifiers for learning/feedback/preference/verification), or (b) HAI_item_id is correctly the single canonical identifier and other surfaces should not be cross-referenced. **S1800 D80 posture-decision depends on this distinction.**

**Retention posture DIVERGENT across primitives** (extends S1805 §20.7):

| Primitive | Retention | Owner |
|-----------|-----------|-------|
| HAI_item_id | SAVED-FOREVER + bounded (spider_action 6h + arbitrage 12h) | Cat A |
| feedback_record_id | SAVED-FOREVER (no cleanup) | Cat B |
| learning_event_id | SAVED-FOREVER (no cleanup) | Group 1300 Memory |
| user_pref_id | SAVED-FOREVER (no cleanup) | Cat D |
| verification_id (HAI status VERIFIED) | SAVED-FOREVER (no cleanup) | Cat E |

Contrast: LLMCallEvent has explicit 30-day `CELERY_TASK_EVENT_RETENTION_DAYS` baseline. **Retention divergence** suggests primitives are scoped domain-internally and retention is not a cross-domain concern; if they became cross-system, retention would need standardization.

**xx99 §10 promotion wording candidate:**

> **MC-3 correlation-primitive HYPOTHESIS box discipline: CODIFICATION-CONFIRMED (utility-rate framing).** Applied 5 times across Group 1800 child audits (S1801-S1805) on 5 distinct model families (HumanAttentionItem, HumanFeedbackRecord, AgentLearning, HumanPreference, HumanAttentionItem verification tier). All 5 applications produced defensible verdicts: 1 genuine cross-system primitive (HAI_item_id, 8-10 domain usage) + 4 domain-internal primitives (confirmed via exhaustive grep + ORM spot-checks). Discipline utility: 100%. Discipline pass rate: 20%. **Discipline shall be codified NOT on pass-rate basis, but on utility-rate basis.** The 4 failures are CORRECT DIAGNOSES that the modeled primitives are domain-internal — not design failures of the discipline. Corollary: xx99 shall frame the discipline as "verify whether a primitive is cross-system (PASS) or domain-internal (FAIL) — both are valid outcomes." Primitive abundance under Group 1800 is **1 heavy-hitter (HAI_item_id) + 4 intentional domain-internals.** This is likely the correct distribution. Future primitives should be tested similarly. MC-3 promotable to CODIFICATION-CONFIRMED for methodology robustness, not for outcome pass-rate.

**Alternatives considered + rejected:**

1. **Deprecate the discipline (1/5 pass rate looks bad)** — REJECTED. Utility-rate framing shows discipline works; pass-rate framing is misleading.
2. **Add a "mode" classifier to distinguish cross-system-design-gap from cross-system-not-required** — CONSIDERED but REJECTED. Adds interpretation burden. Binary PASS/FAIL is simpler; child audits can note mode distinctions in findings if needed.
3. **Retire the primitive box and switch to a "retention-posture audit" that's simpler** — REJECTED. Primitives and retention are orthogonal concerns.

### 20.8 D80 posture-decision brief evidence consolidation (for xx99 §5 consumption)

D80 asks: **"Is the human-in-the-loop attention queue (HumanAttentionItem) the canonical learning-signal aggregation surface, OR are learning bridges autonomous domain-specific consumers that bypass HAI?"** Four options per parent §2.5.

**Option A — Canonical Unification (HAI is canonical learning aggregator):**

*SUPPORT:* S1274 §4.7 + S1802 §1 canonical round-trip narrative; S1801 §15.1 D1 producer diversity (43 code paths across 14+ domains); S1804 governance-plane functionality; unified decision-event timestamp on HAI.record_decision().

*UNDERMINE:* S1803 §1 100% autonomous bridge writer plane (zero of 9 bridges touch HAI); S1801 §9.2 + S1274 §3.3 four MISSING cross-domain integrations still missing at HEAD; S1801 F2 producer-side personalization absence; S1804 F4 + S1805 F4 durable-at-two learning-loop decoupling; S1805 F3 sports-only verification; S1803 §4.1 ZERO AgentLearning writers from bridges; producer-side signal cost (43 producers already overloaded).

**Option B — Structural Separability (HAI is display queue; bridges are autonomous learning consumers):**

*SUPPORT:* S1803 §1 100% autonomous bridge design at HEAD; S1274 §4.7 reinterpretation (dual round-trips); domain-specific learning signals (SportsBettingLearningBridge reads odds/bankroll); S1801 F2 producer-preferences-display-only makes sense under separability framing; operational autonomy (bridges don't require HAI coordination); zero MISSING integrations urgency under separability.

*UNDERMINE:* S1274 §4.7 canonical narrative wording (singular "only round-trip"); S1801 §15.1 governance tie-in (HumanPreference gates apply to HAI); learning state coherence (schema-split suggests fragmented Unification not intentional Separability); consumer confusion (asymmetric read/write surfaces per S1804 F7 + S1805 F7/F8); S1801 D5 + S1805 F4 coupling suggests intended interconnection.

**Option C — Hybrid (HAI canonical for approval/verification; bridges canonical for outcome/attribution):**

*SUPPORT:* Semantic split plausible; S1803 §4.1 writer-plane split already exists; S1805 F3 sports-only verification precedent; eliminates unification scope-creep.

*UNDERMINE:* Boundary ambiguity (watch vs verification); HAI scope creep; learning schema incoherence; governance-only-bridge issue; S1274 canonical narrative singular framing.

**Option D — Shared Surface (Unified learning-event schema):**

*SUPPORT:* Eliminates incoherence (S1803 R8 candidate); enables federated queries; compatibility with all terminology boundaries; analogous to S1703 F9 shared correlation view precedent.

*UNDERMINE:* Migration risk (schema merge); semantic overload; performance trade-off; not required for D80 resolution; S1299 Memory arc design precedent (Group 1300 chose two-model split).

**Cat F consolidated framing constraint (F7 CRITICAL):** All four options are BLOCKED at HEAD by learning-loop coupling breaks (S1804 F4 + S1805 F4 + S1801 D5). xx99 §5 posture-decision brief must EITHER frame D80 selection as "with-assumptions-pending-fix" OR defer post-T-slot.

### 20.9 Miss-vector rule-out grep extensions (per S1805 §20.9 discipline extension + Rigby SIGN cycle 1 Q1 miss-vector probes)

**Post-SIGN grep sweep landed pre-commit per Rigby SIGN cycle 1 Q1 miss-vector probes:**

**Probe 1: Non-*Learning*-suffix shadow services** — `rg -c "^class \w+(Loop|Ingest|Enricher|Reconciler|Attribution)\b" --type py`:

| Hit | File:line | Verdict |
|-----|-----------|---------|
| `AutonomousIntelligenceLoop` | `core/services/autonomous_loop.py:22` (Session 460) | NOT a shadow learning service — spider-to-Discord orchestration pipeline; correctly excluded from F.d |
| `LearningLoop` | `ai_core/intelligence/learning_loop.py` | Consumed BY `RedditLearningBridge`; NOT a separate service — part of F.a ecosystem |
| `PersonalizationBridge` etc. (7 bridges) | `core/learning_bridges/*.py` | ALREADY CATALOGUED at S1803 Cat C §3.2 as 9 core LearningBridges — not miss-vector |
| `SpiderQualityTracker` | `intelligence/spider_quality_tracker.py:1` | Attribution-adjacent; NOT a learning service — quality metric tracker |
| `RevenueVerifier` | `revenue/revenue_verifier.py:1` | Attribution-adjacent verifier; NOT a learning service per se; may be F.b cross-domain candidate (revenue verification chain) |
| `DataEnrichment` | `sports/data_enrichment.py:1` | Sports-domain enrichment; NOT a learning service |
| **`PAToolLearningEnricher`** | **`core/services/pa_tool_learning_enricher.py:22`** | **MISS-VECTOR HIT** — a genuine additional PA-tool learning-plane READER not previously catalogued in F.c. Reads `PAToolInsight` model (same as `learning_tool` handler writes). Registered in INTENT_ENRICHMENT_MAP under 'tool_learning' + appended to function-calling system prompt. Extends F.c PA-tool learning surface to TWO reader services (PALearningInsightsService reads Experiment/ExperimentLearning + PAToolLearningEnricher reads PAToolInsight). Boundary discipline STILL CLEAN — reads only, no HAI/Feedback/Preference/AgentLearning writer plane touch. Adds credibility to F.c overall verdict but adjusts scope from 1 PA-tool-learning-surface to 2. |

**Probe 2: Non-obvious PA-tool learning routes triggering downstream writes** — Verifier scope: `learning_tool` writes `PAToolInsight.safety_class` (already catalogued at §3.3); `remember_tool` writes `UserMemory` (already catalogued); `feedback_tool` `.save()` at `td_handlers_content.py:3538` scope pending R9 verification. **No additional PA-tool learning-plane WRITERS found.** F.c write-plane boundary CLEAN.

**Probe 3: Additional cross-domain HAI-consumer gaps in other desks** — `rg -c "HumanAttentionItem.objects.create\(|create_.*_attention\(" --type py`: 40+ files matching, matching S1801 §1 Finding 1 "43 distinct HAI producer code paths across 14+ domains" — no new domain producers surfaced beyond S1801 inventory. **F.b MISSING gap set (5 verified + 3 uncataloged candidates) is likely stable at this scope depth**, but Rigby's "additional gaps likely in other desks" caveat retained per F2 softening fold.

**Probe 4: "settlement/outcome" terminology alternative** — `rg -c "settlement" core/models_human_interface.py core/services/betting_outcome_verifier.py` returns ZERO hits. "Settlement" is NOT used as alternative to "verification" in Cat E files at HEAD. F.e.1 5-semantic-terms + verification-as-HAI-field posture holds. Rigby's speculative 6th-term suggestion CORRECTLY RULED OUT.

**Miss-vector rule-out summary:**

- Probe 1: 1 GENUINE HIT (`PAToolLearningEnricher`) — added as F.c catalog extension at §3.3 + F5 finding scope update below.
- Probes 2, 3, 4: NO HITS. Rigby's miss-vector concerns RULED OUT under scanned patterns; caveats retained for unscanned surfaces.

**F5 finding scope extension (post-SIGN Q1 fold):** F.c PA-tool learning-plane READER inventory extended from 1 → 2 services. `PALearningInsightsService` @ `core/services/pa_learning_insights.py:24` (447 LOC) reads `Experiment` + `ExperimentLearning`; `PAToolLearningEnricher` @ `core/services/pa_tool_learning_enricher.py:22` reads `PAToolInsight` (safety_class='approved' filter + expires_at check). Both are READ-ONLY context injectors; both boundary-CLEAN with respect to HAI/Feedback/Preference/AgentLearning writer plane per S1605 tactical-split discipline. Extended F.c topic-doc discoverability fold: `PAToolLearningEnricher` also NOT enumerated in `docs/topics/personal-assistant.md` enrichment table — inherits R4 discoverability fix scope.

### 20.10 D48 arm state — Rigby SIGN worker instability tracking

**D48 30th arm start** at S1806 open. **D48 30th arm turn 1 CLEAN** (Rigby SIGN cycle 1 single-batch 4-question returned High 0.82 confidence + specific F# edit recommendations without stall or generic error signature). **25th consecutive-fully-clean-arms sub-pattern CONFIRMED** per single-batch 4-question criterion (S1799 §10.2 MC-2 CODIFICATION-READY promotion path continues advancing toward CODIFICATION-CONFIRMED at 25-consecutive-fully-clean-arms milestone). SIGN routing via arc pin `pa-ae5931ea706b4537` per S1801-S1805 arc-pin routing precedent — **arc-pin routing NOW durable-by-sixth-application across six-child-arcs** (established as arc-standard behavior; will document explicitly at Group 1800 xx99 close).

**S1806 close CONFIRMS the 25-consecutive-fully-clean-arms milestone.** MC-2 CODIFICATION-CONFIRMED promotion path is achieved at S1806 close; xx99 §10 lands the promotion under playbook v3 §20 two-triggers threshold satisfied at 25-consecutive-clean-arm precedent.

### 20.11 Playbook §11.2 20-section child template ELEVENTH application overall + SIXTH under Group 1800

Cat F is the ELEVENTH application overall of playbook §11.2 20-section child template + SIXTH under Group 1800. Template durability confirmed at S1806 open per parent template pre-Explore verifier (all 20 section headings applied uniformly). Playbook §11.2 continues durable-at-eleven-applications pattern.

**Cross-arc §11.2 20-section application history:**
1. S1601 (Group 1600 Content Cat A)
2. S1701 (Group 1700 Observability Cat A)
3. S1602 (Group 1600 Cat B)
4. S1702 (Group 1700 Cat B)
5. S1801 (Group 1800 Cat A HAI Core — first under Group 1800)
6. S1802 (Group 1800 Cat B FeedbackProcessor)
7. S1803 (Group 1800 Cat C Learning bridges)
8. S1804 (Group 1800 Cat D HumanPreference)
9. S1805 (Group 1800 Cat E S746 verification loop)
10. S1806 (Group 1800 Cat F Adjacent / Separation Boundaries — THIS DOC; sixth under Group 1800; eleventh overall)

**Note: S1806 is the LAST child under Group 1800.** After S1806 close, Group 1800 transitions from In-progress to Awaiting summary; xx99 S1899 canonical summary owes close as the immediate next mission per §5 P7 slot.

### 20.12 SIGN cycle 1 record

**Rigby SIGN cycle 1 single-batch 4-question per playbook §15 stage-table on arc pin `pa-ae5931ea706b4537` — LANDED at S1806 2026-07-04:**

- **Overall confidence:** **High (0.82)**
- **Most accurate part:** "The consolidation correctly identifies the shape of the problem: multiple learning-ish planes (human feedback, autonomous bridges, external signal aggregators, shadow learning services) with weak provenance, duplicate class-name collisions, and a durable false-confidence loop pattern. F3/F4/F7 are well-chosen as arc-close leverage points, and the separation vs integration distinction at F.a is framed thoughtfully."
- **Weakest part:** "Any exhaustive completeness claim in a consolidation audit is the easiest to over-claim. The most likely miss-vectors are (a) additional learning-ish services named `*Loop`, `*Ingest`, `*Enricher`, `*Reconciler`, (b) non-obvious PA-tool pathways that touch learning indirectly, and (c) additional cross-domain HAI-consumer gaps not in the human_attention namespace."
- **Rigby verdict:** **SIGN-with-edits**
- **F1-Fn folds applied pre-commit** (per §20.6 fold record): F1 intentional-separation + dead-hook reframing / F2 verified-set + candidate-set + search-scope softening / F3 silent-import-collision mechanics explicitness / F4 no-canonical-writer-evidence + no-ADR-endorsement / F7 phantom-learning-loop 2-line tie-back / §1 biggest-risk RISK-SPLIT paragraph + canonical-plane one-paragraph statement / F.b maturity WORKING → PARTIAL / F.d WORKING explicit-qualification / §19 R-slot next-step-gating line / §20.9 miss-vector rule-out grep extended (Probe 1 surfaced GENUINE HIT `PAToolLearningEnricher` @ `core/services/pa_tool_learning_enricher.py:22` extending F.c reader inventory 1→2 services; Probes 2-4 no-hit).

**End of S1806 Cat F child audit doc.** Committing artifact set next.
