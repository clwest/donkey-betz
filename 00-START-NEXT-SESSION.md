# Next Session — Start Here

---

## READ THIS — SESSION 3051 CLOSED. **RaaS UI Phase 2 PR 3 SHIPPED — Gaps 1/5 discharged.**

S3051 shipped PR 3 of the 5-PR Phase 2 slice: new `/my` route + minimal `<CustomerLayout>` variant + bare chat page. **Zero-hallucination Rigby SIGN streak restored** (T1 AGREE 5/5 with 5 substantive `repo_tool` runs, all 3 nits folded pre-code). Chris ratified both deviations (`paChat` vs `.chat`, no component test — no frontend test infra) via Rigby dispatch. Clean recycle, live-verified.

**HEAD at close:** `3084bb7ce` (PR #3816). Docs-cascade PR for handoff + 00-START + wrapper pin bump follows.

### What shipped

- **PR #3816 (`3084bb7ce`)** — `/my` route + `<CustomerLayout>` + `MyPage`. New files: `frontend/src/components/layout/CustomerLayout.tsx` (+43), `frontend/src/pages/MyPage.tsx` (+199). Modified: `frontend/src/App.tsx` (+17). `core/templates/index.html` auto-regenerated by frontend postbuild (bundle `index-Dugl7I4R.js`).
- **CustomerLayout** — minimal header (logo + email + Sign out) + `<Outlet />`. NO Sidebar, NO GlobalAlertBanner, NO DemoModeBanner, NO GlobalPADock, NO operator telemetry hooks.
- **MyPage** — bare chat: thread + composer + async `paChat`/`paChatStatus` polling with `resolved` guard (mirrors GlobalPADock:358), interval cleanup on unmount, `conversation_id` capture for thread continuity. Uses canonical `paChat` (approved deviation from spec-literal `.chat`).
- **Recycle event** — `sha=3084bb7cef9c`, `surviving=none`. Clean.
- **Live E2E verification** — `GET /my` → HTTP 200; bundle hash synced between `dist/index.html` and `core/templates/index.html`; 6 `/my` references in bundle.
- **37th consecutive Cycle 1A verify-before-build session.**

### PLAYBOOK folds

**Zero folds.** PR 3 shipped cleanly — spec revision, no `same_pr_mitigatable` refinements, no `future_trigger` observations. Rigby's 3 nits were implementation-level (folded same-envelope), not shape-level folds.

### Rigby SIGN streak note

**Restored.** T1 SIGN produced substantive `content` field + AGREE verdict + 5 real `repo_tool` runs (Layout.tsx contents, no existing `/my`, no `MyPage`, api.ts surface, GlobalPADock polling pattern). The S3050 empty-final-text pattern stays filed as 2nd trigger in `5c84e75a-…`; discharge threshold unchanged.

---

## S3052 first-action — RATIFIED PICK (Chris yes/no's at open)

### Pick: **Open PR 4 of RaaS UI Phase 2 — role guard (`CustomerRoute` + `IsOperatorRole` DRF permission)**

**Provenance:** S3049 close ratified the 5-PR slice; S3050 shipped PRs 1+2; S3051 shipped PR 3. Chris's exact signal at PR 3 close (via Rigby dispatch): *"treat PR 4 as the next hard gate."*

### PR 4 spec (from §5 of `edf69671-…`)

- **Scope:** Extend `ProtectedRoute` at `frontend/src/App.tsx:49-61` to check `user.platform_role` (**verified S3049**: exists on User model directly, alongside `customer_role`, `subscription_tier`, `tenant`). Add `CustomerRoute` variant that redirects operator-only paths back to `/my` when caller is customer-role. Server-side companion: add `IsOperatorRole` DRF permission class + apply to operator-only endpoints.
- **Effort:** S–M (was M before authz-primitives verification)
- **Blocking for demo:** yes — Chris named this "the next hard gate"
- **Discharges:** Gap 6 (auth only checks `isAuthenticated`, not role)
- **No schema change needed** (mirrors Gap 7 win from PRs 1-3).

### Concrete opening move (S3052)

1. `context-kit orient` (auto-injected)
2. Absorb this file + `MEMORY.md` + `CLAUDE.md`
3. Read S3051 handoff (`docs/handoffs/SESSION_3051_S3051_RAAS_UI_PHASE_2_PR3_CUSTOMER_LAYOUT_SHIPPED.md`)
4. **Cycle 1A verify-before-build FIRST** — re-run `build_pa_tool_audit --gap-only --check` to confirm RaaS-validated=163; ORM-verify `User.platform_role`/`customer_role`/`subscription_tier`/`tenant` still present; verify PRs #3813 (`dcca75fae`) + #3814 (`9c2cde312`) + #3816 (`3084bb7ce`) still in HEAD. **38th consecutive Cycle 1A session.**
5. Read `edf69671-…` §5 PR 4 spec via `deliverable_tool.get` (or ORM chunked read).
6. Grep for existing `Is*Permission` classes in `core/` to identify DRF permission-class conventions (reuse vs. new).
7. Grep for existing role checks on the frontend (`user.platform_role`, `user.customer_role` references) to identify where the redirect logic should be threaded (Layout / ProtectedRoute / Login post-auth flow).
8. **T1 SIGN + A2 SIGN if class-scoped drift.** PLAYBOOK-7.7.5 **DOES fire** if PR 4 closes the "auth check doesn't consider role" class of drift — enumerate at-minimum 4 sweep dimensions (other route guards / adjacent Redirect wrappers / server permission gaps / tests that lock in old auth-only behavior).
9. Chris ratifies scope at PR envelope, not just at session open.

### Rejected candidates (documented for provenance)

- **Skip to PR 5 (tool allowlist)** — rejected; PR 5 without PR 4 is moot (operators can still reach `/my` and customer chat has all 163 tools). PR 4 is the load-bearing gate.
- **Cascade PR 4 + PR 5 in single session** — deferred; PR 4 has substrate risk (DRF permission class + route-tree extension) that warrants own SIGN cycle. Consider cascading only if PR 4 lands cleanly with runway budget.
- **Pivot to `/docs/` restructuring arc** — still deferred (Chris directive S2800).
- **Meta-work on Rigby Tool Gap Ledger 2nd-trigger** — defer until 3rd trigger or Rigby-side instrumentation opens.

---

## S3052 carry-forward seeds

### New from S3051

- **PR 3 shipped** (Gaps 1/5 discharged). PR 4 unblocked and **named as next hard gate by Chris.**
- **Zero-hallucination Rigby SIGN streak restored** — T1 AGREE 5/5 with substantive tool_runs. Empty-final-text 2nd trigger from S3050 remains open.
- **CustomerLayout + MyPage** shipped as canonical customer-shell primitives. Reference for PR 4 role guard: any redirect must land customers on `/my`, not `/`.
- **No PLAYBOOK-7.7.5 fire** — PR 3 was capability-add. PR 4 is **drift-closure class** (auth check drift → role-aware auth) so 7.7.5 fires at S3052.

### Carried from prior arcs — status preserved

- **PRs 4 + 5 remaining** in Phase 2 slice (S-M and M respectively).
- **Odds API operationally degraded** — Chris directive S3045: no active API key; back burner.
- **Skiplist re-validation** (5 media/audio tools) — deferred.
- **`agent_router.py:2131-2132` silent fallback** — 1st `future_trigger` (S3043).
- **T1 Fold future_trigger (`typing.Literal[actor]`)** — 1st trigger (S3036).
- **A2 Fold future_trigger (actor-taxonomy vs frontend-palette drift)** — 1st trigger (S3036).
- **`did_X` semantics** — 2nd trigger (S3034).
- **S3033 Fold B** — ledger persistence timing (1st trigger discharged; watch for 3rd).
- **S3030 prod deploy carry** — `backfill_canonical_drift --apply` on Railway prod.
- **S3032 Fold E** — `orm_inspect_tool` allowlist accretion.
- **S3031 Fold B** — spy fragility.
- **S3034 A2 Folds** — subscriber wire-contract fragility + adjacent-axis superseded/experiment.
- **S3042 arc Q3/Q4** — Spine Contract v1 §§1+3 (frontend event instrumentation + Workspace UI redo Arc C).
- **`chris-personal` orphan-initiative cleanup pass** — Spine Contract v1 §4.
- **Stem-matcher warn-only lint (Option B)** — S3044 row 1; 3-trigger threshold reached; deferred per Rigby T0 SIGN Q5.
- **`/docs/` restructuring arc** — queued (Chris directive S2800).
- **T2 spec for `agent_router.py:2131-2132` silent fallback** — deferred.
- **Provenance test suite 4 failures** — surfaced S3048; still deferred.
- **Rigby Tool Gap Ledger row `5c84e75a-…` (empty-final-text)** — 2nd trigger filed S3050; discharge threshold: instrumentation OR 3rd trigger.
- **Frontend test infra arc** — flagged S3051 as follow-on (vitest + testing-library introduction so future frontend PRs can ship component tests). Non-urgent; no current test coverage assumed anywhere.
- **S3049 Phase 3+ deferrals** — Gap 3, Gap 8, full role UI, permission matrices, ACL editor, fine-grained ABAC.

---

## Cross-cutting workflow references

- **Constitutional governance chain:** CLAUDE.md Playbook **v0.11.0**. No amendments this session.
- **Cycle 1A verify-before-build:** **37th consecutive session.**
- **Claude directs, Rigby executes, Claude verifies:** followed cleanly. T1 SIGN dispatched, tool_runs verified, then coding.
- **SIGN evidence discipline (PLAYBOOK-7.7.2):** Rigby's tool_runs substantive + verdict substantive + 3 nits folded pre-code.
- **PLAYBOOK-7.7.5 (drift-closure class-scoped sweep):** did NOT fire (capability-add class). **Expected to fire at S3052 for PR 4** (role-aware auth = drift closure of auth-only check).
- **Recycle discipline (PLAYBOOK-7.4.4):** `make recycle-all` after merge (frontend touched — S2978 refinement). Clean.
- **Local truth (`feedback_local_truth_no_production`):** local recycle + curl verification. Live-verified post-recycle.
- **`--admin` on merges:** used per Chris directive S2750.

---

## Wrapper pin note

Active PA conversation pin at S3051 close: `pa-37ca4b0807634fb2` (minted mid-session for Chris ratification dispatch; verify with `session_lifecycle status`). `session_lifecycle close` at close time atomically retires it + mints next-session pin + rewrites `tools/pa_local.sh`. Commit wrapper diff per `feedback_commit_wrapper_pin_bump_at_close`.

---

**Reminder — the workflow is constitutional.** S3051 shipped 1 PR (capability-add class, no PLAYBOOK-7.7.5 fire, zero folds, streak restored). Chris named PR 4 the next hard gate. S3052 opens with PR 4 role guard; expect PLAYBOOK-7.7.5 A2 sweep to fire on drift-closure class shape signature (`isAuthenticated`-only route/permission gates).
