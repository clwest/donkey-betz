# Generated by Django 5.0.6 on 2025-11-27 19:10
# Session 452: Fixed - Made state-only since 0029 is now state-only
# The RemoveIndex and AlterField may not need to touch the database since
# the previous migration didn't actually create the index or change the type.

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("content", "0030_add_sequential_number_to_imagehistory"),
    ]

    operations = [
        # Session 452: Use SeparateDatabaseAndState since 0029 didn't actually
        # create the index (it was state-only), so we can't remove it.
        migrations.SeparateDatabaseAndState(
            state_operations=[
                migrations.RemoveIndex(
                    model_name="documentembedding",
                    name="embedding_hnsw_idx",
                ),
                migrations.AlterField(
                    model_name="documentembedding",
                    name="embedding_vector",
                    field=models.JSONField(
                        help_text="The actual embedding vector as JSON array. Supports pgvector upgrade."
                    ),
                ),
            ],
            database_operations=[
                # No-op - index doesn't exist (0029 was state-only), field is already JSON
            ],
        ),
        migrations.AlterField(
            model_name="minifigasset",
            name="project",
            field=models.ForeignKey(
                blank=True,
                help_text="Creative project this 3D model belongs to",
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="three_d_models",
                to="content.creativeproject",
            ),
        ),
    ]
